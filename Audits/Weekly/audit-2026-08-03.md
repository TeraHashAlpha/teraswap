# TeraSwap Weekly Security Audit — 2026-08-03

**Overall Status:** 🟡 WARNING

> Auditoria **report-only**. Nenhuma alteração foi aplicada à working copy: `package.json` e
> `package-lock.json` permanecem intactos (`git diff HEAD -- package-lock.json` = 0 linhas).

## npm Audit

- **Vulnerabilities:** 0 critical, 1 high, 9 moderate, 0 low (total 10)
- Nenhuma delas é uma dependência de produção directa em runtime — todas entram por
  `@capacitor/cli` (tooling), `eslint`/`typescript-eslint` (dev) ou pela cadeia
  `wagmi → @wagmi/connectors → @metamask/sdk` (browser wallet connectors).

| Package | Severity | Via | Fix disponível | Nota |
|---------|----------|-----|----------------|------|
| `brace-expansion` | 🔴 high | GHSA-mh99-v99m-4gvg (DoS por expansão ilimitada, OOM) | sim, non-major | Resíduo em `minimatch@3.1.5` → `brace-expansion@1.1.16` (range vulnerável `<1.1.17`). O override de `minimatch@10.2.5` → `5.0.8` já está correcto (PR #386). |
| `tar` | 🟡 moderate | GHSA-r292-9mhp-454m (recursão descontrolada, stack-overflow DoS) | sim, non-major | `tar@7.5.20` via override; só `@capacitor/cli`. |
| `uuid` | 🟡 moderate | GHSA-w5hq-g745-h8pq (falta bounds check em v3/v5/v6) | só via `wagmi@3.7.5` (**major**) | Cadeia MetaMask SDK. |
| `@metamask/utils`, `@metamask/rpc-errors`, `@metamask/sdk`, `@gemini-wallet/core`, `@wagmi/connectors`, `wagmi` | 🟡 moderate | transitivos do `uuid` acima | só via `wagmi@3.7.5` (**major**) | Cluster único — fecha todo com o upgrade wagmi v2→v3. |
| `@metamask/sdk-communication-layer` | 🟡 moderate | `uuid` | sim, non-major | — |

### Safe fixes available (report-only)

- `brace-expansion` → `1.1.17` (via ajuste do override existente para `minimatch@3.1.5`) — **fecha o único HIGH**
- `tar` → `>7.5.20`
- `@metamask/sdk-communication-layer` → patch disponível

**Não-safe (major, manual):** 8 de 10 findings só fecham com `wagmi@2.19.5 → 3.7.5`
(semver major). Não recomendado como fix automático — wagmi v3 é o mesmo salto que já
causou o incidente das 4 WC Cores ([`project_walletconnect_jun03`], P184). Exige ADR + sprint próprio.

**Route:** Dependabot / PR manual — esta auditoria **não** aplica fixes. `npm audit fix` não foi executado.

## Lockfile Integrity

✅ **No issues detected** — todos os packages vêm do registry npm via HTTPS
(`lockfile-lint --allowed-hosts npm --validate-https`). `.npmrc` confirmado com
`ignore-scripts=true`, `save-exact=true`, `min-release-age=7`, registry fixado em
`https://registry.npmjs.org/`.

## Outdated Packages

| Package | Current | Wanted | Latest | Type |
|---------|---------|--------|--------|------|
| next | 16.2.11 | 16.2.11 | 16.2.12 | patch |
| eslint-config-next | 16.2.11 | 16.2.11 | 16.2.12 | patch |
| @next/swc-darwin-arm64 | 16.2.6 | 16.2.6 | 16.2.12 | patch |
| viem | 2.55.2 | 2.55.2 | 2.55.10 | patch |
| postcss | 8.5.22 | 8.5.22 | 8.5.25 | patch |
| @upstash/redis | 1.38.0 | 1.38.0 | 1.38.1 | patch |
| @rainbow-me/rainbowkit | 2.2.10 | 2.2.10 | 2.2.11 | patch |
| @capacitor/browser | 8.0.3 | 8.0.3 | 8.0.4 | patch |
| @capacitor/splash-screen | 8.0.1 | 8.0.1 | 8.0.2 | patch |
| @capacitor/status-bar | 8.0.2 | 8.0.2 | 8.0.3 | patch |
| @capacitor/android · cli · core · ios | 8.3.4 / 8.4.1 / 8.4.2 / 8.4.1 | = | 8.5.0 | minor |
| @sentry/nextjs | 10.59.0 | 10.59.0 | 10.69.0 | minor |
| @supabase/supabase-js | 2.108.2 | 2.108.2 | 2.112.0 | minor |
| @tanstack/react-query | 5.50.1 | 5.50.1 | 5.101.4 | minor |
| framer-motion | 12.36.0 | 12.36.0 | 12.43.0 | minor |
| @playwright/test | 1.61.1 | 1.61.1 | 1.62.1 | minor |
| **wagmi** | 2.19.5 | 2.19.5 | **3.7.5** | **major** |
| **react / react-dom** | 18.3.0 | 18.3.0 | **19.2.8** | **major** |
| **@types/react / @types/react-dom** | 18.3.31 / 18.3.7 | = | **19.2.18 / 19.2.4** | **major** |
| **typescript** | 5.9.3 | 5.9.3 | **7.0.2** | **major** |
| **tailwindcss** | 3.4.19 | 3.4.19 | **4.3.3** | **major** |
| **eslint** | 9.39.5 | 9.39.5 | **10.8.0** | **major** |
| **zustand** | 4.5.7 | 4.5.7 | **5.0.14** | **major** |
| **valtio** | 1.13.2 | 1.13.2 | **2.3.2** | **major** |
| **jsdom** | 29.1.1 | 29.1.1 | **30.0.1** | **major** |
| **@types/node** | 20.19.43 | 20.19.43 | **26.1.2** | **major** |
| **@testing-library/jest-dom** | 6.9.1 | 6.9.1 | **7.0.0** | **major** |

Nada foi actualizado. `Wanted == Current` em toda a linha — o lockfile está coerente com
os ranges declarados; os deltas são todos `latest`. Os `@next/swc-*` marcados `MISSING`
são binários por plataforma (linux/win) ausentes num checkout macOS — normal, não é finding.

## Git Activity

`git fetch origin` bem-sucedido. **86 commits** em `origin/main` nos últimos 7 dias
(46 non-merge). Checkout local sincronizado com `origin/main` (0 ahead / 0 behind,
HEAD = `bed8db5`, 2026-08-01).

Temas da semana:

- **Oracle fail-closed / Chainlink (ADR-018)** — auto-identificação de feed (`8415cca`),
  remediação de 7 feeds mainnet mal configurados (`fef052a`), regressão fail-open em
  `useChainlinkPrice` corrigida (`35fac22`), fail-closed na criação de DCA (`d16e30d`),
  fail-closed na leitura de preço (`c9d6e27`), política de staleness p/ heartbeat 24h (`bad4a7e`),
  achados L-1/L-2 do painel DCA fechados (`a3cebb8`)
- **Keeper hardening** — verificação de chain + identidade do executor no boot, fail-closed
  (`2e5acbb`), redacção de segredos em todas as formas (`9d9db6e`), linearidade de `redactUrls`
  por rácio (`7716e5a`), job `keeper-suite` como required check (`2513596`)
- **Supply chain** — pin de `brace-expansion` 5.0.8 fechando GHSA-mh99-v99m-4gvg (`290f553`),
  override reescopado por major de minimatch após quebrar o eslint (`a9252a4`),
  install de produção determinístico com `npm ci` (`7e1f615`), pins do `codeql-action` sincronizados (`b1da190`)
- **CI/lint** — reparação do flat-config, `.eslintrc.json` morto retirado (`0960d2e`),
  registo do plugin react-hooks (`d0aa5ad`), job full-suite com coverage floor (`8dbde65`)
- **Depeg gate** na criação de ordens (`15c09ca`) + cobertura in-handler (`04cbc75`)
- **Dependabot merges** — `@capacitor/core` 8.4.1→8.4.2 (#352), grupo dev-dependencies (#351)

## Build Status

⚠️ **Não verificável neste ambiente — inconclusivo (não é falha do código).**

`npm run build` abortou com `EPERM: operation not permitted, unlink
'.next/diagnostics/build-diagnostics.json'` — o sandbox da auditoria monta a working copy
sem permissão de unlink, e os binários `@next/swc-linux-*` não estão instalados num
checkout macOS. Acresce que o shell da auditoria tem um tecto de 45 s por chamada,
insuficiente para um `next build` completo.

Sinais substitutos recolhidos:

- ✅ **`tsc --noEmit` passa limpo** (0 erros) — a árvore de tipos está sã
- ⏱️ `eslint .` excedeu o tecto de 45 s (timeout, não erro) — inconclusivo
- ✅ O build a falhar **não** deixou resíduo: `git status` sem ficheiros tracked modificados

Cobertura real do build: workflow `ci` no push/PR para `main` (8 workflows activos).
Recomenda-se confirmar o último run verde de `ci` em `bed8db5`.

## Disk Space

- Volume do projecto (host): **461G total, 403G usados, 58G livres — 88%** → ⚠️ **WARNING** (>85%)
- Volume do sandbox: 9.6G / 5.4G usados (56%) — ✅ (irrelevante para o host)

## Uncommitted Dependency Changes

✅ **0 linhas** de diff em `package-lock.json`. Sem instalações manuais por commitar.

Untracked em `main` (esperado, não é finding): `Audits/Daily/health-2026-07-31.md`,
`Audits/Daily/health-2026-08-01.md`, `Audits/Monthly/security-2026-08.md`,
`contracts/order-engine/broadcast/`, `.claude/settings.local.json`. Os relatórios de cadência
vivem no branch `audits/cadence` (confirmado: `0c0bac3` daily 2026-08-02) — aparecem
untracked em `main` por desenho.

## Action Required

- [ ] **Fechar o único HIGH** — rever PR (Dependabot ou manual) que leve `brace-expansion`
      a `1.1.17` no ramo `minimatch@3.1.5`. O override em `package.json` já está
      escopado por major (`"minimatch@3.1.5": { "brace-expansion": "1.1.16" }`) — basta
      subir o pin. Impacto: dev-only (eslint), mas fecha o gate.
- [ ] Rever PRs Dependabot abertos para os restantes safe fixes: `tar`, `@metamask/sdk-communication-layer`.
- [ ] **Decisão de arquitectura:** `wagmi` v2→v3 fecha 8 dos 10 findings de uma vez, mas é
      semver major com histórico de regressão (WC Cores duplicadas, P184). Não fazer como
      bump de rotina — merece ADR próprio e sprint dedicado.
- [ ] **Disco do host a 88%** — libertar espaço antes de cruzar os 95%. Candidatos:
      `.next/`, `node_modules/` de worktrees antigos, caches Foundry.
- [ ] Confirmar `ci` verde em `bed8db5` (build não verificável nesta auditoria).
- [ ] Patches de rotina disponíveis e sem risco conhecido: `next`/`eslint-config-next`
      16.2.11→16.2.12, `viem` 2.55.2→2.55.10, `postcss`, `@upstash/redis`, `rainbowkit`.
- [ ] **Commitar este relatório manualmente** — o passo PERSIST falhou (ver abaixo).
- [ ] Housekeeping (ajuda no disco a 88%): `git worktree prune` — 10+ worktrees em
      `/private/tmp/*` marcadas `prunable`, resíduo dos PRs paralelos desta semana.

---

## PERSIST — falhou

`node scripts/commit-audit-report.mjs` terminou com erro. Conforme a regra do runbook,
**não houve retry, stash nem force**; o erro fica registado aqui e o commit passa a manual.

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
(status 128)
```

**Causa:** o sandbox da auditoria não tem a chave de assinatura SSH configurada, e por
política os agentes nunca leem o keychain nem invocam credential helpers. O commit tem de
ser assinado (regra #12 do CLAUDE.md), portanto a assinatura só pode acontecer numa sessão
do owner.

Efeitos colaterais: **nenhum**. O branch continua `main`, `git status` não mostra ficheiros
tracked modificados, e a worktree temporária do script não ficou pendurada
(`git worktree list` limpo desse ponto de vista). O relatório está em disco, untracked.

**Passo manual:** correr `node scripts/commit-audit-report.mjs` a partir de uma sessão com a
chave de assinatura disponível — vai apanhar `Audits/Weekly/audit-2026-08-03.md` e empurrar
para `audits/cadence`.

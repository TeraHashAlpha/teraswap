# TeraSwap Weekly Security Audit — 2026-06-22

**Overall Status:** 🟡 WARNING

> Execução automática (tarefa agendada `teraswap-weekly-audit`). Sem alterações ao `main`. Nenhuma escrita git efetuada — ver nota de ambiente abaixo.
>
> ⚠️ **Mudança face à semana passada:** surgiram **5 novas vulnerabilidades `high`** (a auditoria de 2026-06-15 tinha 0 `high`). Todas transitivas/dev e já cobertas pela *allowlist* do `audit-gate.mjs` no CI — ver secção npm Audit e Action Required.

## npm Audit
- Vulnerabilities: **0 critical, 5 high, 29 moderate, 1 low** (35 no total)
- Auto-fix applied: **No — não executado.** Sem fix limpo e seguro; bloqueadores de ambiente. Ver justificação abaixo.
- Build after fix: **N/A**
- Remaining (manual): as 5 `high` + 29 `moderate` + 1 `low` — detalhe abaixo.

### As 5 novas `high` (esta semana)

| Pacote | Severidade | Origem (transitiva) | Fix disponível |
|--------|-----------|---------------------|----------------|
| `form-data` (4.0.0–4.0.5) | high | CRLF injection em multipart; `node_modules/form-data` | ✅ patch (via `overrides`) |
| `undici` (7.0.0–7.27.2) | high | TLS bypass / cache disclosure / header injection; transitiva (Sentry/Next/Supabase) | ✅ patch (via `overrides`) |
| `ws` (7.x / 8.0.0–8.20.1) | high | DoS / memory disclosure; via `@walletconnect/*` e `engine.io-client` | ⚠️ só com `wagmi@3` (major) |
| `hono` (≤4.12.24) | high | múltiplas (CORS/cookies/path traversal); dev/tooling transitiva | ✅ patch (via `overrides`) |
| `vite` (8.0.0–8.0.15) | high | `server.fs.deny` bypass (Windows); dev tooling | ✅ patch (via `overrides`) |

As 29 `moderate` + 1 `low` correspondem à *baseline* conhecida WalletConnect/Reown/wagmi (`@reown/appkit*`, `@walletconnect/*`, `viem`, `@metamask/*`, `@opentelemetry/*`, `@sentry/*`) — cadeia transitiva, sem risco de produção (ver [Dependabot Baseline] na memória).

### Porque é que o auto-fix NÃO foi executado

O `npm audit fix` (sem `--force`) **não produz um patch limpo** neste repositório:

1. **Não resolve as `high` de forma segura** — o plano calculado pelo npm puxa um **major novo de `vite`/`rolldown`** (`add @rolldown/binding-* 1.0.3`, `@napi-rs/wasm-runtime`, etc.), ou seja, substitui o stack de build em vez de aplicar um patch pontual. Não é um *fix* seguro.
2. **`ws` (high)** só resolve com `wagmi@3.6.17` (`isSemVerMajor: true`) — fora do âmbito de fix automático; tratado em ADR-008.
3. **As `high` corrigíveis por patch** (`form-data`, `undici`, `hono`, `vite`) resolvem-se de forma cirúrgica via `overrides` no `package.json` — mecanismo que o projeto já usa (`axios`, `@walletconnect/*`, `postcss`) e que está **agendado para conversão por volta de hoje (~Jun-22)** segundo a nota [WC/Reown advisory] (mover `form-data`/`vite` da *allowlist* para `overrides`).

**Bloqueadores adicionais de ambiente (sandbox), que tornam o commit impossível mesmo que houvesse fix limpo:**

- **Sem assinatura de commits** — `commit.gpgsign`, `gpg.format` e `user.signingkey` estão vazios nesta sandbox. Um commit não assinado viola a regra #12 do `CLAUDE.md` e seria rejeitado pela *branch protection* do `main`.
- **Escritas git bloqueadas** — o `.git/index.lock` não pode ser removido (`unable to unlink … Operation not permitted`); qualquer `git checkout -b` / `git commit` falharia. O FS montado não permite `unlink`.
- **Branch suja / fora do `main`** — a branch atual é `docs/inc-2026-06-09`, com ficheiros modificados (sub-pacote `contracts/order-engine/executor`, submódulo OpenZeppelin) e dezenas de `Audits/Daily/*` não rastreados.

Decisão autónoma: **reportar e recomendar a via `overrides`** (segura, cirúrgica, já planeada) em vez de arriscar um auto-fix destrutivo. Sem `critical`; as `high` são transitivas/dev e já suprimidas no gate de CI pela *allowlist*.

## Lockfile Integrity
✅ All packages from npm registry via HTTPS — `lockfile-lint` sem problemas (`✔ No issues detected`).

## Outdated Packages
| Package | Current | Wanted | Latest | Type |
|---------|---------|--------|--------|------|
| next | 16.2.6 | 16.2.9 | 16.2.9 | patch (safe) |
| @next/swc-darwin-arm64 | 16.2.6 | 16.2.6 | 16.2.9 | patch (safe) |
| eslint-config-next | 16.2.6 | 16.2.6 | 16.2.9 | patch (safe) |
| vitest | 4.1.7 | 4.1.7 | 4.1.9 | patch (safe) |
| framer-motion | 12.36.0 | 12.36.0 | 12.40.0 | minor (review) |
| viem | 2.47.4 | 2.47.4 | 2.53.1 | minor (review) |
| @sentry/nextjs | 10.53.1 | 10.53.1 | 10.59.0 | minor (review) |
| @rainbow-me/rainbowkit | 2.1.0 | 2.1.0 | 2.2.11 | minor (review) |
| @supabase/supabase-js | 2.99.1 | 2.99.1 | 2.108.2 | minor (review) |
| @tanstack/react-query | 5.50.1 | 5.50.1 | 5.101.0 | minor (review) |
| @capacitor/core·cli·ios | 8.2.0 | 8.2.0 | 8.4.1 | minor (review) |
| @capacitor/android | 8.3.4 | 8.3.4 | 8.4.1 | minor (review) |
| wagmi | 2.19.5 | 2.19.5 | 3.6.17 | major (breaking — manual, ADR-008) |
| react / react-dom | 18.3.0 | 18.3.0 | 19.2.7 | major (breaking — manual) |
| @types/react / -dom | 18.3.0 | 18.3.0 | 19.2.x | major (breaking — manual) |
| @types/node | 20.19.41 | 20.19.41 | 26.0.0 | major (breaking — manual) |
| typescript | 5.9.3 | 5.9.3 | 6.0.3 | major (breaking — manual) |
| eslint | 9.39.4 | 9.39.4 | 10.5.0 | major (breaking — manual) |
| tailwindcss | 3.4.19 | 3.4.19 | 4.3.1 | major (breaking — manual) |
| zustand | 4.5.7 | 4.5.7 | 5.0.14 | major (breaking — manual) |
| valtio | 1.13.2 | 1.13.2 | 2.3.2 | major (breaking — manual) |
| @tsparticles/react·slim | 3.x | 3.x | 4.2.1 | major (breaking — manual) |

Nenhuma atualização aplicada (apenas relatório, conforme a especificação da tarefa). `wagmi@3` continua alinhado com ADR-008 — sprint dedicado, nunca bump avulso. Os majors `next/swc-linux-*` aparecem como `MISSING` porque o lockfile foi resolvido em darwin-arm64 (esperado).

## Git Activity
2 commits nos últimos 7 dias (semana de baixa atividade — fecho de sprint + runbooks de deploy Base):

- `df00d35` audit: SPRINT-201 DCA observability/freeze APPROVED (0C/0H/0M/1L/6I)
- `1d6c52b` docs: Base deploy/KMS/EC2 runbooks + DEPLOYMENTS.md + chore prompts

Foco da semana: aprovação da auditoria do SPRINT-201 (observability/freeze do DCA, veredito 0C/0H) e documentação operacional do deploy em Base (KMS/EC2, `DEPLOYMENTS.md`).

## Build Status
❌ **Não verificável neste ambiente — não é falha de código.** O `next build` arrancou e compilou (via *fallback* WASM, pois o binário nativo `@next/swc-linux-arm64-*` não está instalado nesta sandbox Linux — o lockfile tem o binário macOS arm64) e abortou apenas em:

```
Error: EPERM: operation not permitted, unlink '.next/BUILD_ID'
```

Mesma causa-raiz do bloqueio git: o FS montado não permite `unlink`. Nenhum erro de compilação/tipos antes do EPERM. **A verificação real de build é o CI/Vercel** (o projeto está LIVE em produção). Correr `npm run build` no ambiente local do TeraHash se for preciso confirmação manual.

## Disk Space
✅ 55% usado (`/dev/nvme0n1p1` — 5.2G / 9.6G, 4.4G livres). Abaixo do limiar de 85%.

## Uncommitted Dependency Changes
- ✅ Lockfile raiz: `git diff HEAD -- package-lock.json` = **0 linhas** (sem install manual pendente na raiz).
- ⚠️ **Sub-pacote**: `contracts/order-engine/executor/package.json` e `package-lock.json` estão **modificados e por confirmar** (pré-existente nesta branch `docs/inc-2026-06-09`). Confirmar se é alteração intencional do keeper/executor antes do próximo ciclo.

## Action Required
- [ ] **PRIORIDADE — converter `form-data` e `vite` (+ `undici`, `hono`) para `overrides`** no `package.json` (mover da *allowlist* do `audit-gate.mjs`). Já agendado para ~Jun-22 na nota [WC/Reown advisory]. Resolve 4 das 5 `high` de forma cirúrgica e sem major. Fazer localmente, em branch, com commit assinado:
  ```bash
  git checkout main && git pull
  git checkout -b chore/weekly-audit-fix-20260622
  # editar package.json → adicionar overrides para form-data, undici, hono, vite (versões corrigidas)
  npm install
  npm audit            # confirmar high resolvidas (exceto ws)
  npm run build        # verificar
  git commit -S -m "chore(deps): pin form-data/undici/hono/vite via overrides — 2026-06-22"
  ```
- [ ] **`ws` (high)** — só resolve com `wagmi@3` (`isSemVerMajor`). Tratar dentro da migração ADR-008, não como bump avulso. Entretanto, a *allowlist* do CI mantém o gate verde.
- [ ] **29 `moderate` + 1 `low`** — baseline transitiva WalletConnect/Reown (sem risco de produção). Sem ação imediata; desaparecem com `wagmi@3`.
- [ ] **Patches seguros** (opcional, baixo risco): `next 16.2.6→16.2.9`, `vitest 4.1.7→4.1.9`, `eslint-config-next →16.2.9`.
- [ ] **Sub-pacote executor por confirmar** — rever `contracts/order-engine/executor/package*.json` modificados e committar/arrumar.
- [ ] **Não foi criada branch de auto-fix** — ambiente sem assinatura de commits (regra #12) e com escrita git bloqueada (`.git/index.lock` EPERM). O fix tem de correr no ambiente local do TeraHash.

---
*Gerado automaticamente por `teraswap-weekly-audit`. Revisão humana recomendada para os itens acima. Verificação de build = CI/Vercel (autoridade), não esta sandbox.*

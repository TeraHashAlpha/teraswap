# TeraSwap Daily Health Report — 2026-06-11

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200 (308 → www.teraswap.app), 1.84s response (com redirect) |
| API Health | ✅ | HTTP 200, 0.87s response — `{"status":"OK"}` |
| SSL Certificate | ✅ | Expires 2026-07-14 (33 days remaining) |
| Monitor | ⚠️ | `{"error":"Unauthorized"}` — MONITOR_SECRET presente no .env local mas rejeitado pelo endpoint |
| Git Activity | ℹ️ | 0 commits in last 24h |
| KV / Rate Limiter | ✅ | Inferido da disponibilidade da API (health endpoint não expõe estado KV sem `?token=HEALTH_TOKEN`) |

## Actions Taken

None required — sem retries necessários (todas as respostas finais 200 após seguir o redirect 308 apex → www).

## Requires Attention

- **Monitor endpoint (⚠️):** o `/api/monitor` devolve `Unauthorized` com o `MONITOR_SECRET` do `.env` local. Causas prováveis: secret rodado no Vercel sem atualizar o `.env` local, ou alteração no esquema de auth do endpoint. Verificar manualmente o valor em Vercel → Environment Variables e sincronizar o `.env`.
- **Nota (informativo):** o curl direto a `https://teraswap.app` devolve 308 (redirect permanente para `www.teraswap.app`). Comportamento esperado da config Vercel/DNS, mas o check do task file espera 200 sem `-L` — considerar atualizar o check para seguir redirects ou apontar diretamente para `www`.

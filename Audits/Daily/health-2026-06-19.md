# TeraSwap Daily Health Report — 2026-06-19

**Overall Status:** 🟢 NORMAL

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200 (após redirect canónico 308 apex→www), ~0.70s de resposta |
| API Health | ✅ | HTTP 200, ~0.23s, `status: OK` |
| SSL Certificate | ✅ | Expira a 11 Set 2026 (83 dias restantes) |
| Monitor | ✅ | Sem anomalias, sem eventos de segurança (`recentEvents: []`, todos os z-scores = 0) |
| Git Activity | ℹ️ | 0 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido saudável a partir da disponibilidade da API |

## Detalhes

**Disponibilidade.** O domínio apex `teraswap.app` devolve `HTTP 308` (redirect permanente canónico) para `https://www.teraswap.app/`, que responde `HTTP 200`. Comportamento esperado — o site está totalmente operacional. Tanto a homepage como `/api/health` (`status: OK`) respondem dentro do esperado (< 1.1s seguindo o redirect). Não foi necessário retry: o 308 é determinístico e não indica indisponibilidade.

**SSL.** Certificado válido até 11 Set 2026 — 83 dias restantes, muito acima dos limiares de WARNING (< 14 dias) e CRITICAL (< 7 dias).

**Monitor (`/api/monitor`).** `HTTP 200` com dados completos. Sem alertas: `failRate24h: 0`, `volumeZScore: 0`, `walletDominance: 0`, e `security.recentEvents` vazio. Source health sem falhas em todas as fontes (`failureRate: 0.0` em kyberswap, cowswap, velora, uniswapv3, odos, openocean). Atividade das últimas 24h: 0 trades / 0 falhas (período tranquilo, dentro do normal). All-time: 39 trades, ~$301.61 de volume, 8 wallets, 4 falhas históricas.

**Git.** Sem commits nas últimas 24h.

## Actions Taken

Nenhuma ação corretiva necessária — todas as verificações passaram. Nenhum processo foi reiniciado, nenhum código modificado, nenhum commit efetuado (bot read-only, exceto este relatório).

Nota técnica: a primeira chamada ao `/api/monitor` devolveu `HTTP 401` porque, ao seguir o redirect apex→www, o `curl` remove o header `Authorization` num salto entre subdomínios diferentes. Repetida diretamente contra `https://www.teraswap.app/api/monitor`, a autenticação foi preservada e devolveu `HTTP 200`. Não é uma falha de segurança nem de autenticação — apenas um artefacto do redirect.

## Requires Attention

Nada crítico — tudo operacional.

Recomendação menor (não bloqueante): em execuções futuras, apontar a verificação do monitor diretamente para `https://www.teraswap.app/api/monitor` (ou usar `curl --location-trusted`) para evitar o falso `401` causado pela remoção do header `Authorization` no redirect apex→www.

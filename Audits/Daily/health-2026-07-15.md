# TeraSwap Daily Health Report — 2026-07-15

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.42s |
| API Health | ✅ | HTTP 200, 0.38s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~58 dias) |
| Monitor | ⚠️ | HTTP 401 — stale-local-secret: `MONITOR_SECRET` local desatualizado (rodado no Vercel, não atualizado em `.env.local`). Gap de verificação LOCAL, não outage de produção. |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH na Base — acima do limiar WARNING (0.01), mas com margem mínima; top-up recomendado em breve |
| DCA execution | ⚠️ | Não verificável — Supabase devolve "Invalid API key": `SUPABASE_SERVICE_ROLE_KEY` local está stale (atualizar `.env.local`). Gap de verificação LOCAL, não falha do keeper. |
| Git Activity (origin/main) | ℹ️ | 0 commits nas últimas 24h (fetch OK, origin/main verificado) |
| KV / Rate Limiter | ✅ | Inferido saudável — API health HTTP 200 |

## Actions Taken
None required — site e API responderam 200 à primeira tentativa; sem retries. Keeper não foi tocado (read-only).

## Requires Attention
1. 🟡 **`MONITOR_SECRET` local stale** — atualizar em `.env.local` com o valor atual do Vercel para restaurar a verificação do endpoint `/api/monitor`.
2. 🟡 **`SUPABASE_SERVICE_ROLE_KEY` local stale** — atualizar em `.env.local`; enquanto não for corrigido, o health check diário está cego à execução DCA (keeper corre LIVE na Base sem verificação independente por esta via).
3. ℹ️ **Gas do keeper com margem mínima** — 0.01149 ETH, apenas ~15% acima do limiar WARNING. Considerar top-up do executor 0x71f5…2E5 na Base.
4. 🟡 **Persist falhou** — `scripts/commit-audit-report.mjs` errou (ver abaixo); 8 relatórios continuam por commitar em `audits/cadence` (07-09 a 07-15 + weekly 07-13). Requer execução manual num ambiente com a signing key configurada.

## Persist Error (commit-audit-report.mjs)

Exit code 1 — o commit no worktree temporário falhou por falta de chave de assinatura no ambiente sandbox:

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
warning: unable to unlink '.git/objects/…/tmp_obj_*': Operation not permitted (×3)
```

O script detetou 8 relatórios por persistir: Daily 2026-07-09 → 2026-07-15 e Weekly audit-2026-07-13. Não foi feito retry, stash nem force (conforme regras). Ação manual: correr `node scripts/commit-audit-report.mjs` na máquina do owner (signing key SSH disponível).

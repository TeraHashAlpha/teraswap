# TeraSwap Daily Health Report — 2026-04-22

**Overall Status:** 🟡 WARNING

## Resultados

| Check | Status | Detalhes |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200 (via 307 redirect), 0.52s resposta |
| API Health | ✅ | HTTP 200 (via 307 redirect), 1.63s resposta. Body: `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira 14 Jul 2026 (83 dias restantes) |
| Monitor | ⚠️ | HTTP 200 mas retorna `{"error":"Unauthorized"}` — possível dessincronização do MONITOR_SECRET |
| Git Activity | ℹ️ | 6 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido a partir da disponibilidade do API Health (status OK) |

## Detalhes Git (últimas 24h)

6 commits — maioritariamente migração de `@vercel/kv` para `@upstash/redis` e correcções associadas:

- `3339bef` fix(test): alinhar testes de threshold cowswap com config actual
- `6a7a6b4` chore(kv): migrar de @vercel/kv para @upstash/redis (P62)
- `d8f456a` test(kv): actualizar mocks de testes de @vercel/kv para @/lib/kv (P61)
- `7923e1f` refactor(kv): mudar todos os imports de @vercel/kv para @/lib/kv (P60)
- `42f5f5a` feat(kv): adicionar módulo centralizado de cliente KV (P61)
- `ec2c2bc` fix(watchdog): adicionar flag -L e URL canónico www para redirect 307

## Notas Técnicas

- **Redirect 307:** Ambos os endpoints (site e API) retornam 307 antes de chegar ao 200. Este comportamento é esperado (Vercel redirect de domínio apex para `www`). O fix `ec2c2bc` do watchdog já endereça isto com a flag `-L`.
- **Monitor Unauthorized:** O endpoint `/api/monitor` retorna `{"error":"Unauthorized"}` apesar de carregar `MONITOR_SECRET` do `.env`. Possíveis causas: (1) o secret no `.env` local pode estar dessincronizado com o valor em produção após a rotação de secrets do incidente Vercel (2026-04-21); (2) o `.env` pode não conter a variável `MONITOR_SECRET`. Requer verificação manual.

## Actions Taken

- Retentativas não foram necessárias — todos os endpoints responderam na primeira tentativa.
- Seguidos redirects 307 com flag `-L` para obter status final.

## Requires Attention

- ⚠️ **MONITOR_SECRET:** Verificar se o `MONITOR_SECRET` no `.env` local corresponde ao valor em produção (Vercel). Contexto: rotação completa de secrets realizada a 2026-04-21 após o incidente de breach Vercel. O commit `ec2c2bc` corrigiu o `MONITOR_CRON_SECRET` no Worker, mas o `MONITOR_SECRET` do `.env` local pode necessitar re-sincronização.

# TeraSwap Daily Health Report — 2026-06-01

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 307 → 200 (redirect `www`), 0.51–0.75s |
| API Health | ✅ | HTTP 307 → 200, 0.79s, `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira 14 Jul 2026 (43 dias restantes) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — segredo presente mas rejeitado |
| Git Activity | ℹ️ | 7 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido saudável a partir da disponibilidade da API |

## Detalhe das verificações

**1. Disponibilidade do site.** A raiz `https://teraswap.app` devolve HTTP 307 (redirecionamento normal para `https://www.teraswap.app/`), que resolve em HTTP 200. Tempo de resposta final 0.51–0.75s — bem abaixo do limiar de 3s. Sem problema; o 307 é o comportamento esperado do redirect `www`.

**2. API Health.** `/api/health` segue o mesmo redirect e devolve HTTP 200 com corpo `{"status":"OK","timestamp":"2026-06-01T07:06:24Z"}`. Tempo 0.79s.

**3. Certificado SSL.** Válido até 14 Jul 2026 07:15 GMT — **43 dias** restantes. Acima do limiar de aviso (14 dias). Normal.

**4. Monitor endpoint.** `/api/monitor` devolveu **HTTP 401 Unauthorized** (`{"error":"Unauthorized"}`). A variável `MONITOR_SECRET` existe no `.env` local e tem valor (64 caracteres), mas o servidor rejeitou-o. Indica desfasamento entre o segredo local e o configurado em produção (Vercel), ou rotação de segredo não propagada. Classificado como WARNING conforme a regra do bot (o monitor pode estar em manutenção; não é crítico). Nenhum segredo foi exposto durante a verificação.

**5. Atividade Git (24h).** 7 commits:
- `9d72f4b` fix(quote): harden /api/quote so it can never return HTML 502 [INC-2026-05-31-001]
- `3d938d4` feat(bebop): add Bebop (JAM) as the 12th chain-aware source [SPRINT-9D/P228]
- `c8ca8b1` fix(adapters): make uniswapv3 + curve chain-aware — no mainnet quote on Base [SPRINT-9C]
- `98e9df0` feat(quote): add admin read-only debug=sources per-source diagnostic
- `64e68ca` fix(base): accept any active chain in swap flow checks
- `74b5a80` Merge PR #114 (sprint-45-base-activation)
- `0945a51` feat(base): wire Base FeeCollector to NEXT_PUBLIC_BASE_FEE_COLLECTOR env

**6. KV / Rate Limiter.** O endpoint `/api/health` não expõe estado KV detalhado sem `?token`. Saúde inferida a partir da disponibilidade da API (HTTP 200), considerada saudável.

## Actions Taken

- Site/API devolveram inicialmente HTTP 307 (non-200): executadas 3 tentativas adicionais à raiz — todas 307 consistentes; confirmado por `curl -L` que o destino final é HTTP 200. Não é uma falha, é o redirect `www` normal.
- Verificada a presença (não o valor) de `MONITOR_SECRET` no `.env` para caracterizar o 401. Nenhuma escrita, reinício de processo ou commit efetuado (bot read-only).

## Requires Attention

- **Monitor 401 (WARNING).** Verificar se o `MONITOR_SECRET` em produção (Vercel) coincide com o do `.env`. Possível rotação de segredo não sincronizada. Reexecutar a verificação após reconciliar; se persistir 401 por vários dias, escalar — o endpoint de monitorização está efetivamente cego.

# TeraSwap Daily Health Report — 2026-06-08

**Overall Status:** 🟢 NORMAL

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200 (307→www, esperado), 0.75s response |
| API Health | ✅ | HTTP 200, `{"status":"OK"}`, 0.77s response |
| SSL Certificate | ✅ | Expira Jul 14 2026 (35 dias restantes) |
| Monitor | ✅ | HTTP 200, sem anomalias, 0 eventos de segurança |
| Git Activity | ℹ️ | 0 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Saudável (inferido da disponibilidade da API) |

## Detalhes

**Disponibilidade.** O domínio apex (`teraswap.app`) responde com 307 a redirecionar para `www.teraswap.app`, que devolve 200 — comportamento esperado, não é uma falha. Tempos de resposta abaixo de 1s nos dois endpoints.

**SSL.** Certificado válido até 14 de julho de 2026 (35 dias). Acima do limiar de aviso (14 dias).

**Monitor.** Endpoint a responder 200 com dados completos. Indicadores de anomalia todos a zero (`volumeZScore`, `tradeFreqZScore`, `walletDominance`, `failRate24h`), lista de `alerts` vazia e `security.recentEvents` vazia. Sem atividade de trading nas últimas 24h (0 trades), consistente com o padrão recente de baixo volume. Saúde das fontes sem falhas (`failureRate` 0.0 em todas); de notar tempos de resposta mais altos em `cowswap` (~3.7s) e `odos` (~4.6s), mas sem impacto em falhas — apenas para observação.

**Git.** Sem commits no repositório nas últimas 24h.

## Actions Taken

Nenhuma necessária. Não houve respostas non-200 finais — o 307 do apex resolve para 200 no host `www`, pelo que não foram acionados retries. O pedido inicial ao `/api/monitor` foi reencaminhado (307) e o cabeçalho `Authorization` é removido em redirect; repetido diretamente contra `www.teraswap.app` para preservar o bearer token (escolha automática, sem alteração de credenciais).

## Requires Attention

Nada — tudo em ordem. Observação não-bloqueante: latência de quote elevada em `cowswap` e `odos` (sem falhas associadas); reavaliar se persistir ou degradar.

# TeraSwap Daily Health Report — 2026-07-17

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.34s |
| API Health | ✅ | HTTP 200, 0.36s |
| SSL Certificate | ✅ | Expires 2026-09-11 (~56 dias) |
| Monitor | ⚠️ | 401 Unauthorized — stale-local-secret (`MONITOR_SECRET` local desatualizado vs Vercel; NÃO é outage de produção) |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH on Base (acima do limiar WARNING de 0.01, mas por pouco — top-up recomendado em breve) |
| DCA execution | ⚠️ | "Invalid API key" — `SUPABASE_SERVICE_ROLE_KEY` local stale → check cego (stale-local-key, não é falha do keeper) |
| Git Activity (origin/main) | ℹ️ | 2 commits nas últimas 24h: merge PR #308 (`d029810`) + fix wagmi regista Arbitrum 42161 (`41d6c5f`) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health HTTP 200 |

## Actions Taken
None required — todos os checks de disponibilidade passaram à primeira tentativa (sem retries). Keeper não foi tocado (read-only).

## Requires Attention
1. 🟡 **Atualizar `MONITOR_SECRET` em `.env.local`** — o secret foi rodado na Vercel e a cópia local está stale; o check do endpoint `/api/monitor` está cego até ser atualizado.
2. 🟡 **Atualizar `SUPABASE_SERVICE_ROLE_KEY` em `.env.local`** — chave local inválida; o check de saúde de execução DCA (últimas execuções + ordens ativas) está cego até ser atualizado. Enquanto ambos os checks estiverem cegos, não há visibilidade local sobre o estado do keeper/DCA — resolver hoje se possível.
3. ℹ️ **Gas do keeper perto do limiar** — 0.01149 ETH na Base (limiar WARNING = 0.01). Recomenda-se top-up do executor `0x71f5…2E5` nos próximos dias para evitar alerta.

---
*Notas do run: fetch a origin/main OK (warnings de unlink em .git/objects por permissões do sandbox, sem impacto). Checks 4 e 6 classificados como LOCAL check gap conforme runbook — produção respondeu HTTP 200 em site e API.*

## PERSIST — erro (não retried, conforme instrução)

`node scripts/commit-audit-report.mjs` falhou (exit 1) no commit do worktree temporário:

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
```

O sandbox não tem a signing key SSH configurada (regra #12: commits assinados obrigatórios), pelo que o commit assinado não é possível a partir deste ambiente. **Backlog acumulado: 10 relatórios por persistir** (Daily 2026-07-09 → 2026-07-17 + Weekly audit-2026-07-13) — os runs anteriores também não conseguiram persistir. Ação manual: correr `node scripts/commit-audit-report.mjs` numa sessão com a chave de assinatura configurada (ver `docs/Runbooks/SIGNED-COMMITS.md`).

# TeraSwap Daily Health Report — 2026-05-09

**Overall Status:** 🟢 NORMAL

## Resultados

| Check | Status | Detalhes |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.685s resposta |
| API Health | ✅ | HTTP 200, 1.678s resposta |
| SSL Certificate | ✅ | Expira 14 Jul 2026 (66 dias restantes) |
| Monitor | ⚠️ | HTTP 401 — Unauthorized (MONITOR_SECRET presente mas rejeitado pelo endpoint) |
| Git Activity | ℹ️ | 6 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido da disponibilidade do API Health (status OK) |

## Detalhes dos Checks

### Site & API
Ambos os endpoints responderam com HTTP 200 (após redirect 307 HTTP→HTTPS esperado). Tempos de resposta dentro dos limites normais (< 3s).

### SSL Certificate
Certificado válido até 14 de Julho de 2026 — 66 dias restantes. Sem ação necessária.

### Monitor Endpoint
O endpoint `/api/monitor` retornou HTTP 401 (Unauthorized) apesar do `MONITOR_SECRET` estar configurado no `.env` local (64 caracteres). Possíveis causas:
- Secret local dessincronizado com o valor em produção (Vercel)
- Rotação de secrets após o incidente Vercel (ref: `reference_vercel_breach_2026.md`) pode ter atualizado o valor no servidor sem atualizar o `.env` local

**Recomendação:** Verificar se o `MONITOR_SECRET` no dashboard Vercel corresponde ao valor local.

### Git Activity (últimas 24h)
6 commits registados:

1. `06da23a` — docs(ledger): add ERC-7730 clear-signing metadata for FeeCollector V2
2. `afc42ae` — fix(monitor): match V2 SwapWithFee topic hash + keep V1 backward-compat [9B-I-01]
3. `6196b87` — docs(security): require signed commits + branch protection runbook
4. `7d572b0` — chore(deps): patch axios + hono to clear npm audit HIGH (CI green)
5. `56a8acb` — fix(review): resolve V1→V2 stale references and add V1 to monitor [review findings]
6. `f657a30` — chore(deploy): migrate to FeeCollector V2 with minimumOutput [H-04]

Actividade significativa — deploy do FeeCollector V2, patch de segurança (npm audit HIGH), e fix do monitor para suportar o novo topic hash do V2.

### KV / Rate Limiter
O endpoint `/api/health` retornou `{"status":"OK"}`. Diagnósticos detalhados requerem `HEALTH_TOKEN`. Estado do KV inferido como operacional com base na disponibilidade da API.

## Actions Taken
Nenhuma ação automática necessária. Todos os checks primários passaram.

## Requires Attention

- **⚠️ MONITOR_SECRET dessincronizado** — O endpoint `/api/monitor` rejeita a autenticação. Verificar no dashboard Vercel se o secret corresponde ao valor no `.env` local. Baixa prioridade (não afeta utilizadores), mas impede monitorização completa por este health check.

---
*Relatório gerado automaticamente às 07:06 UTC por TeraSwap Daily Health Check.*

# TeraSwap Daily Health Report — 2026-05-24

**Overall Status:** 🟡 WARNING

## Resultados

| Check | Status | Detalhes |
|-------|--------|----------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.94s resposta (307 redirect → 200 com `-L`) |
| API Health | ✅ | HTTP 200, 1.58s resposta |
| SSL Certificate | ✅ | Expira 2026-07-14 07:15 UTC (51 dias restantes) |
| Monitor | ⚠️ | HTTP 200 mas retornou `{"error":"Unauthorized"}` — MONITOR_SECRET provavelmente ausente ou dessincronizado no `.env` local |
| Git Activity | ℹ️ | 0 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido do API Health — endpoint `/api/health` retornou 200 |

## Notas Técnicas

- **Redirect 307:** Tanto o site como a API retornam HTTP 307 antes de chegar ao 200. Comportamento normal para Vercel com redirect de domínio configurado. `curl` sem `-L` vê apenas o 307.
- **Monitor Unauthorized:** O endpoint `/api/monitor` respondeu com `{"error":"Unauthorized"}`. Isto indica que o `MONITOR_SECRET` no ficheiro `.env` local está ausente, vazio ou dessincronizado com o valor configurado no Vercel. O endpoint em si está funcional (não retornou 500 nem timeout).
- **Tempos de resposta:** Site 0.94s e API 1.58s — ambos dentro dos limites aceitáveis (< 3s).

## Acções Tomadas

- Retry com flag `-L` (follow redirects) após detecção de HTTP 307. Status final: 200 em ambos os endpoints.
- Nenhuma acção correctiva necessária.

## Requer Atenção

- **MONITOR_SECRET local:** Verificar se o ficheiro `.env` na máquina de desenvolvimento contém o `MONITOR_SECRET` correcto e sincronizado com o Vercel. Isto afecta apenas a monitorização local — o endpoint de produção deve estar funcional (confirmar via Cloudflare Worker cron ou Telegram bot).

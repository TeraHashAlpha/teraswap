# TeraSwap Daily Health Report — 2026-05-27

**Overall Status:** 🟡 WARNING

Verificação automática diária executada às 07:06 UTC. Todos os serviços públicos respondem normalmente, mas o endpoint `/api/monitor` devolveu `401 Unauthorized` — provável dessincronização do `MONITOR_SECRET` entre o `.env` local e a configuração em produção (Vercel). Sem impacto utilizador-final; requer rotação/sincronização do segredo.

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200 (após 307 → www.teraswap.app), 0.82s response |
| API Health | ✅ | HTTP 200, 2.02s response (`{"status":"OK"}`) |
| SSL Certificate | ✅ | Expira 2026-07-14 07:15:32 UTC (48 dias restantes) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — `MONITOR_SECRET` provavelmente dessincronizado |
| Git Activity | ℹ️ | 18 commits nas últimas 24h (Sprint 30 fechado, P165–P167, Dependabot batch) |
| KV / Rate Limiter | ✅ | Inferido NORMAL — `/api/health` responde sem erros (sem campo KV explícito no payload) |

## Detalhe das Verificações

### 1. Site Availability
- Raiz `https://teraswap.app` devolve `307` para `https://www.teraswap.app/` (redirect esperado).
- Tempo total de resolução: **0.82s** (< 3s threshold). ✅ NORMAL.

### 2. API Health
- `GET /api/health` → `HTTP 200`, **2.02s**.
- Payload: `{"status":"OK","timestamp":"2026-05-27T07:06:12.186Z","hint":"Provide ?token=HEALTH_TOKEN for detailed diagnostics."}`.
- Tempo dentro do limite (< 3s), mas perto do threshold de WARNING — vale a pena observar tendência nos próximos dias.

### 3. SSL Certificate
- `notAfter=Jul 14 07:15:32 2026 GMT` → **48 dias** até expirar. ✅ NORMAL.
- Sem ação necessária; renovação automática pela Vercel deverá ocorrer ~30 dias antes.

### 4. Monitor Endpoint
- `GET /api/monitor` com `Authorization: Bearer $MONITOR_SECRET` → **HTTP 401** `{"error":"Unauthorized"}`.
- Variável `MONITOR_SECRET` está presente no `.env` local, mas o servidor rejeita o token.
- Hipóteses: (a) segredo rodado em produção sem actualizar `.env` local; (b) header schema mudou; (c) IP allowlist/auth gate adicional na rota.
- ⚠️ Não-crítico (o monitor de Cloudflare/cron continua a operar), mas o agente diário fica cego à telemetria interna até resolver.

### 5. Git Activity (últimas 24h)
**18 commits** entre 2026-05-26 08:21 e 23:03 BST. Destaques:

- `8bcf5c8` — **Sprint 30: Operational backlog cleanup (P165–P167) APPROVED 0C/0H** ✅
- `282b6bf` — `docs(feedback): record P167 Dependabot merge results + npm audit [P167]`
- `c036556`/`efda372`/`236f210`/`5219ecd`/`5aa7cb6` — Merges Dependabot (dev-deps group, Sentry 10.53.1, Capacitor status-bar/android, zustand 4.5.7) [P167]
- `8df724b` — `chore(lint): switch lint script to direct eslint [P166]`
- `b19959d` — `feat(analytics): integrate Vercel Analytics in root layout [P165]`
- `d738112` — Merge PR #90 `activate/fee-collector-v2`
- `ef039de` — `test(v1-swap): re-enable fee-collectable tests [P163]`
- `ca6eb2b` — `chore(fees): revert FEE_INCOMPATIBLE_SOURCES temporaries [P162]`
- `1a20a2d` — Merge PR #89 `perf/sprint-29-optimization`
- Série `perf/*` (P89–P92): self-host de fontes (Inter + JetBrains Mono via `next/font`), preconnect hints, lazy-load below-fold, chunk splitting viem/wagmi.
- `9efaebb` — Merge PR #88 `redesign/cinematic-landing`

Padrão: actividade alta e saudável, todos os commits referenciam prompts (P89–P167) e respeitam a convenção (1 prompt = 1 commit).

### 6. Rate Limiter / Vercel KV
- `GET /api/health` → HTTP 200 (idempotente). Sem campo `kv`/`redis` explícito no payload básico (provavelmente disponível em `?token=HEALTH_TOKEN`, não fornecido).
- Inferência: KV operacional, dado que rate-limited routes não devolvem 5xx. ✅ NORMAL.

## Actions Taken
- Nenhum retry necessário — todos os endpoints públicos responderam 200 no primeiro pedido.
- Nenhuma alteração de código, deploy ou processo (read-only por design).
- Monitor 401 documentado, sem retry adicional (não é falha intermitente — é problema de credencial).

## Requires Attention

1. **🟡 Monitor endpoint `MONITOR_SECRET` dessincronizado** — Verificar se o segredo foi rodado em produção (Vercel env vars) e actualizar o `.env` local em consequência. Comparar `MONITOR_SECRET` em `.env` com o valor activo em `Vercel → Project → Settings → Environment Variables`. Se necessário, rodar novo segredo e propagar para Vercel + Cloudflare Worker + `.env` local. Referência: rotação semelhante ocorreu em [Vercel Breach 2026](../../../memory/reference_vercel_breach_2026.md) (2026-04-21) — usar mesmo runbook.

2. **ℹ️ API Health response time ~2s** — Perto do threshold de 3s. Monitorizar tendência nos próximos relatórios diários; se ultrapassar 3s consistentemente, abrir investigação (cold start? KV latency? upstream RPC?).

3. **ℹ️ Sprint 30 fechado** — Confirmar com Architect que próximos passos (timelocks até 2026-05-29, Sprint 26 V2 switch) entram no planeamento da semana.

---
*Gerado automaticamente pelo agente `teraswap-daily-health` em 2026-05-27 07:06 UTC. Próxima execução: 2026-05-28.*

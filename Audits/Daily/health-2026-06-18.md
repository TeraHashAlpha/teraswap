# TeraSwap Daily Health Report — 2026-06-18

**Overall Status:** 🟢 NORMAL

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200 (308 apex→www, normal), 0.87s response |
| API Health | ✅ | HTTP 200, 0.84s response, `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira 11 Set 2026 (84 dias restantes) |
| Monitor | ✅ | Sem anomalias, sem eventos de segurança, failRate 24h = 0 |
| Git Activity | ℹ️ | 2 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido saudável a partir de API 200 (`/api/health` = OK) |

## Detalhe das verificações

**Site e API.** O pedido bruto a `https://teraswap.app` devolve HTTP 308 — é o redirect permanente normal do apex para `https://www.teraswap.app/`. Seguindo o redirect, tanto a página principal como `/api/health` respondem HTTP 200, ambos abaixo de 1s. Sem retries necessários.

**Certificado SSL.** Válido até 11 de Setembro de 2026 — 84 dias restantes, muito acima do limiar de aviso (14 dias).

**Monitor.** Endpoint `/api/monitor` autenticado com sucesso (acedido diretamente via host `www` para preservar o header Authorization, que o curl descarta no salto apex→www). Resultados:

- `anomalies.alerts`: vazio — nenhum alerta ativo (volume/freq/dominância de wallet/failRate todos a 0).
- `security.recentEvents`: vazio — nenhum evento de segurança.
- Source health: 6 fontes ativas, todas com `failureRate` 0.0%. `kyberswap` lidera win rate (67.9%); `cowswap` (2.3%) e `uniswapv3` (4.5%) têm win rates baixos mas 0% de falhas — perdem em preço, não falham (não é anomalia).
- Atividade: 0 trades nas últimas 24h, 2 nos últimos 7d. Baixa atividade esperada (executor parado, fase pré-L2).

**Git.** 2 commits nas últimas 24h:
- `df00d35` — audit: SPRINT-201 DCA observability/freeze APPROVED (0C/0H/0M/1L/6I)
- `1d6c52b` — docs: Base deploy/KMS/EC2 runbooks + DEPLOYMENTS.md + chore prompts

**KV / Rate Limiter.** Sem endpoint dedicado de estado KV exposto; saúde inferida da disponibilidade da API (`/api/health` HTTP 200 com `status: OK`).

## Actions Taken
Nenhuma ação corretiva necessária. Todos os endpoints responderam 200 à primeira tentativa — sem retries. Bot read-only: nenhum processo reiniciado, nenhum commit, nenhuma alteração de código. (Executor mantido parado conforme instruções — não verificado nem reiniciado.)

## Requires Attention
Nada — tudo OK. Sem itens que exijam intervenção manual.

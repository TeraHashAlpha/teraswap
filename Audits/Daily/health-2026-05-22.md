# TeraSwap Daily Health Report — 2026-05-22

**Overall Status:** 🟢 NORMAL

## Results

| Check | Status | Detalhes |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.64s resposta (redirect 307→200 seguido) |
| API Health | ✅ | HTTP 200, 1.62s resposta |
| SSL Certificate | ✅ | Expira 14 Jul 2026 (53 dias restantes) |
| Monitor | ⚠️ | Endpoint retornou `{"error":"Unauthorized"}` — token MONITOR_SECRET pode estar dessincronizado ou ausente no `.env` local |
| Git Activity | ℹ️ | 6 commits nas últimas 24h (Sprint 27 — redesign landing page, P69–P73) |
| KV / Rate Limiter | ✅ | Inferido da disponibilidade do API Health (HTTP 200) |

## Notas Técnicas

- **Redirect 307:** Tanto o site como o API Health devolvem HTTP 307 antes de resolver para 200. Comportamento normal de Vercel (provavelmente redirect www→apex ou protocolo). Tempos de resposta medidos com follow-redirect.
- **Monitor Unauthorized:** O endpoint `/api/monitor` requer `MONITOR_SECRET` via header Authorization. O `.env` local pode não conter o valor correcto ou o secret foi rotacionado. Isto não indica problema no serviço — apenas que a autenticação local falhou. Verificar se o Cloudflare Worker cron (que usa o seu próprio secret) está a funcionar normalmente.
- **SSL:** 53 dias de margem — confortável. Próxima verificação relevante quando < 14 dias.
- **Git Activity (Sprint 27):** 6 commits de redesign da landing page — hero counters, particles, section rhythm, differentiators, swap preview. Todos prefixados `fix(landing)` ou `redesign(landing)`, sem alterações a contratos ou lógica de swap.

## Actions Taken

Nenhuma acção necessária. Retries efectuados com `-L` (follow redirects) após 307 inicial — todos resolveram para HTTP 200.

## Requires Attention

- **MONITOR_SECRET local:** Verificar se o `.env` contém o valor correcto de `MONITOR_SECRET`. Último sync documentado no incident de Vercel Breach (2026-04-21, commit ec2c2bc). Se o secret foi rotacionado desde então, actualizar o `.env` local e o Cloudflare Worker.
- De resto, tudo operacional.

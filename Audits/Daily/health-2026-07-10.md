# TeraSwap Daily Health Report — 2026-07-10

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.62s |
| API Health | ✅ | HTTP 200, 0.40s |
| SSL Certificate | ✅ | Expires 2026-09-11 (~63 dias) |
| Monitor | ⚠️ | 401 Unauthorized — stale-local-secret (`MONITOR_SECRET` no `.env.local`), repetido desde 2026-07-07 |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH on Base (acima do WARNING 0.01, mas **inalterado há 3 dias** — ver "Requires Attention") |
| DCA execution | ⚠️ | stale-local-key — Supabase REST devolve "Invalid API key" (`SUPABASE_SERVICE_ROLE_KEY` desatualizada); execuções DCA **não verificáveis**, repetido desde 2026-07-07 |
| Git Activity (origin/main) | ℹ️ | 39 commits nas últimas 24h — destaque: **PR #302 merged** (v3 deploy arc, ADR-013), PR #301 (v3 cancel + hardening), PR #300/#297 (Arbitrum config dark launch), AUDIT-V3-PREDEPLOY amendments + DEPLOY-AUTHORIZED |
| KV / Rate Limiter | ✅ | Inferido saudável via API health (HTTP 200) |

## Actions Taken

None required — todos os endpoints responderam à primeira tentativa (sem retries). Verificações executadas em modo read-only; keeper não tocado.

## Requires Attention

1. **🟡 `MONITOR_SECRET` local desatualizado** (repetido desde 2026-07-07, 3.ª corrida consecutiva) — `/api/monitor` devolve 401. Cega a verificação de anomalias. Ação: atualizar `MONITOR_SECRET` no `.env.local` a partir do Vercel.
2. **🟡 `SUPABASE_SERVICE_ROLE_KEY` local desatualizada** (repetido desde 2026-07-07, 3.ª corrida consecutiva) — "Invalid API key" no REST do Supabase. Cega a verificação de execuções DCA e ordens ativas. Ação: atualizar a key no `.env.local` a partir do dashboard do Supabase.
3. **🟡 Gás do keeper INALTERADO há 3 dias** — saldo exatamente 0.01149 ETH (5 casas decimais) em 07-07, 07-09 e hoje → **zero gás gasto em ~72h**. Com a verificação Supabase cega (ponto 2), continua impossível distinguir "sem ordens DCA ativas" (NORMAL) de "keeper parado com ordens pendentes" (seria CRITICAL). A urgência de corrigir o ponto 2 sobe a cada dia — recomenda-se verificação manual do estado das ordens DCA hoje.
4. **ℹ️ Gás próximo do limiar** — 0.01149 ETH está ~15% acima do WARNING (0.01). Preparar top-up do executor 0x71f5 na Base.
5. **ℹ️ PERSIST estruturalmente quebrado neste ambiente** (ver secção abaixo e relatórios de 07-07/07-09) — os relatórios ficam apenas no disco local até limpeza manual dos worktrees `audit-cadence-*` órfãos.

**Resumo:** produção aparenta saudável — site, API, SSL, KV OK; ritmo de desenvolvimento elevado (39 commits/24h, arc v3 ADR-013 merged e DEPLOY-AUTHORIZED). O estado WARNING mantém-se pelos dois segredos locais desatualizados que cegam as verificações 4 (monitor) e 6 (DCA), agravado pelo saldo de gás estático há 3 dias que impede confirmar atividade do motor DCA.

## PERSIST — erro (não repetido, conforme instrução)

`node scripts/commit-audit-report.mjs` falhou de novo; relatório escrito localmente mas **NÃO** commitado/pushed para `audits/cadence`. **Erro diferente do de 07-07/07-09**: o lock órfão de worktree parece ter deixado de bloquear, mas o commit falha agora na assinatura obrigatória:

```
warning: unable to unlink '.../.git/objects/…': Operation not permitted
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
```

Diagnóstico: o sandbox não tem acesso à chave SSH de assinatura (correto por design — regra de higiene de credenciais: agentes nunca leem keychain/credential helpers), pelo que o requisito de commits assinados torna o PERSIST inexequível a partir deste ambiente. Ação sugerida: correr o script manualmente fora do sandbox para commitar os relatórios acumulados (07-07, 07-09, 07-10), ou ajustar o cadence-script/runbook para um caminho de persistência compatível com o sandbox.

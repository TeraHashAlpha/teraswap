# TeraSwap Daily Health Report — 2026-07-12

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.58s |
| API Health | ✅ | HTTP 200, 0.38s |
| SSL Certificate | ✅ | Expires 2026-09-11 (~61 dias) |
| Monitor | ⚠️ | HTTP 401 — `MONITOR_SECRET` local desatualizado (stale-local-secret); gap LOCAL, não outage de produção |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH na Base (acima do limiar 0.01, mas margem mínima) |
| DCA execution | ⚠️ | "Invalid API key" — `SUPABASE_SERVICE_ROLE_KEY` local desatualizada (stale-local-key); execuções e ordens ativas não verificáveis |
| Git Activity (origin/main) | ℹ️ | 0 commits nas últimas 24h (fetch OK) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health (HTTP 200) |

## Actions Taken
None required — site e API responderam 200 à primeira, sem retries. Keeper não tocado (read-only).

## Requires Attention
1. 🟡 **Atualizar `MONITOR_SECRET` em `.env.local`** — check do monitor continua cego (3.º dia consecutivo).
2. 🟡 **Atualizar `SUPABASE_SERVICE_ROLE_KEY` em `.env.local`** — verificação DCA continua cega (3.º dia consecutivo). Com DCA LIVE na Base, é o check mais importante a restaurar.
3. ℹ️ **Gas do keeper (0x71f5) = 0.01149 ETH — valor IDÊNTICO ao de 2026-07-11.** Zero gas gasto em 24h significa que o keeper não executou nenhuma transação. Sem acesso Supabase não é possível distinguir "sem ordens DCA ativas" (normal) de "keeper parado com ordens pendentes" (crítico). Restaurar a chave Supabase resolve a ambiguidade; entretanto, planear top-up.
4. 🟡 **Persistência dos relatórios bloqueada (4.º dia)** — `commit-audit-report.mjs` voltou a falhar (ver erro abaixo). Há agora **4 relatórios por persistir** (2026-07-09 a 2026-07-12) no branch `audits/cadence` — correr o script manualmente numa sessão com a SSH signing key disponível.

## Persist Error (commit-audit-report.mjs)

Mesma causa dos dias anteriores — a SSH signing key não está configurada/acessível no sandbox:

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
(+ warnings "unable to unlink .git/objects/**/tmp_obj_*: Operation not permitted")
```

Não foi feito retry, stash nem force, conforme convenção.

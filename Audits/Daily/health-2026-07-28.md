# TeraSwap Daily Health Report — 2026-07-28

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.38s |
| API Health | ✅ | HTTP 200, 0.35s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~45 dias) |
| Monitor | ⚠️ | HTTP 401 "Unauthorized" — MONITOR_SECRET local desatualizado (rodado em Vercel, não sincronizado em `.env.local`); não é outage de produção |
| Keeper gas (0x71f5) | ⚠️ | 0.00736 ETH em Base — abaixo do limiar WARNING (0.01 ETH), acima do CRITICAL (0.002 ETH) |
| DCA execution | ⚠️ | Chave `SUPABASE_SERVICE_ROLE_KEY` local inválida ("Invalid API key") — gap de verificação local, não indica falha do keeper |
| Git Activity (origin/main) | ℹ️ | 19 commits nas últimas 24h (inclui #358 dca-min-chunk-default, #357 ADR RPC endpoints explícitos, #354 fix DCA min-buy copy, #356 RPC transport hygiene, #355 audit-gate brace-expansion, #353 deps-batch-7, #348/#349) |
| KV / Rate Limiter | ✅ | Inferido via disponibilidade da API (HTTP 200) |

## Actions Taken

Nenhuma — todos os checks HTTP devolveram 200 à primeira tentativa (sem necessidade de retries). Nenhum processo foi reiniciado ou modificado, conforme regra de read-only sobre o keeper.

## Requires Attention

1. **Keeper gas baixo (0.00736 ETH em Base)** — está acima do limiar CRITICAL mas abaixo do WARNING (0.01 ETH). Recomenda-se reabastecer a wallet `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` nos próximos dias para evitar que fique sem gás e pare de executar chunks DCA.
2. **`.env.local` desatualizado** — `MONITOR_SECRET` e `SUPABASE_SERVICE_ROLE_KEY` locais estão dessincronizados dos valores em produção (Vercel), cegando dois checks (Monitor e DCA execution). Recomenda-se atualizar `.env.local` com os valores atuais do Vercel para restaurar visibilidade nestes checks em execuções futuras. Isto é um gap de verificação local — não há evidência de outage em produção.
3. Não foi possível confirmar o estado das ordens DCA ativas nem o timestamp da última execução devido ao gap acima — recomenda-se re-executar o check assim que a chave for atualizada.

## Persist Error

`node scripts/commit-audit-report.mjs` falhou: `fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured`. O ambiente sandbox não tem a chave de assinatura SSH configurada para commits assinados. O script também reportou 4 relatórios pendentes de ciclos anteriores ainda não persistidos (health-2026-07-25/26/27.md, audit-2026-07-27.md), acumulados pelo mesmo motivo. Nenhuma tentativa adicional, stash ou force foi feita, conforme instrução. Requer configuração manual da chave de assinatura no ambiente onde a task corre, ou execução do commit pelo owner.

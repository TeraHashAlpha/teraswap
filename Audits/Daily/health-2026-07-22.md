# TeraSwap Daily Health Report — 2026-07-22

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.49s |
| API Health | ✅ | HTTP 200, 0.37s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~51 dias) |
| Monitor | ⚠️ | 401 Unauthorized — `MONITOR_SECRET` local desatualizado (stale-local-secret); não é outage de produção |
| Keeper gas (0x71f5) | ⚠️ | 0.00741 ETH na Base (< 0.01 threshold; acima do crítico 0.002) |
| DCA execution | ⚠️ | Não verificável — `SUPABASE_SERVICE_ROLE_KEY` local inválida ("Invalid API key") → stale-local-key; NÃO é falha do keeper |
| Git Activity (origin/main) | ℹ️ | 41 commits nas últimas 24h — merges #323–#329 (P1b Limit+TP v3/ADR-014, DCA budget UX, audit-gate sharp, Arbitrum DCA prep, mass-cancel DCA fix, go-live docs OE_V3 Base + FC Arbitrum) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health HTTP 200 |

## Actions Taken
None required — todos os endpoints responderam à primeira tentativa; sem retries.

## Requires Attention
1. 🟡 **Keeper gas baixo:** 0x71f5…a2E5 tem 0.00741 ETH na Base. Recarregar para ≥0.01 ETH antes que se aproxime do limiar crítico (0.002 ETH), sobretudo com o volume de execuções DCA + Limit/TP v3 a subir pós-#327.
2. 🟡 **Secrets locais desatualizados (2):** `MONITOR_SECRET` e `SUPABASE_SERVICE_ROLE_KEY` em `.env`/`.env.local` estão stale (rotacionados na Vercel/Supabase e não replicados localmente). Isto cega dois checks diários — monitor e saúde de execução DCA. Atualizar `.env.local` com os valores atuais.
3. ℹ️ **Visibilidade DCA em zero:** enquanto a key Supabase local não for atualizada, não há confirmação independente de que o keeper está a executar ordens ativas. Prioridade acrescida dado que a DCA está LIVE na Base.

---
*Verificações read-only; keeper não foi tocado. Relatório gerado automaticamente (teraswap-daily-health).*

## PERSIST — erro do commit-audit-report.mjs
O script falhou no ambiente sandbox (sem retry, conforme instrução):

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
(warnings: unable to unlink .git/objects/*/tmp_obj_* — Operation not permitted)
```

Causa: a sessão sandbox não tem a signing key SSH configurada (regra #12 — commits assinados obrigatórios). Commit manual pelo owner necessário: `node scripts/commit-audit-report.mjs` num terminal local.

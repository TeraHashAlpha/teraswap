# TeraSwap Daily Health Report — 2026-07-25

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.63s |
| API Health | ✅ | HTTP 200, 0.36s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~48 dias) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — `MONITOR_SECRET` local desatualizado (foi rodado no Vercel, não atualizado em `.env.local`). Gap de verificação local, não indica outage em produção. |
| Keeper gas (0x71f5) | ⚠️ | 0.00736 ETH em Base (abaixo do limiar WARNING de 0.01 ETH; acima do CRITICAL de 0.002 ETH) |
| DCA execution | ⚠️ | Não verificável — Supabase devolveu "Invalid API key" em ambas as queries (`order_executions` e `orders`). `SUPABASE_SERVICE_ROLE_KEY` local desatualizada. Gap de verificação local, não indica falha do keeper. |
| Git Activity (origin/main) | ℹ️ | 1 commit nas últimas 24h: `d08ca02` — merge do PR #347 (`fix/client-ip-cloudflare-aware`) |
| KV / Rate Limiter | ✅ | Inferido saudável via disponibilidade da API (/api/health = 200) |

## Actions Taken

Nenhuma — todas as verificações correram sem necessidade de retry (site e API responderam 200 à primeira tentativa). Nenhuma ação de escrita foi executada (leitura apenas, conforme instruções).

## Requires Attention

1. **Keeper gas baixo (0.00736 ETH em Base)** — abaixo do limiar de aviso (0.01 ETH). Ainda não crítico, mas recomenda-se topar a wallet `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` antes de atingir 0.002 ETH, para evitar que o keeper fique sem gas para executar chunks DCA.
2. **`MONITOR_SECRET` desatualizado em `.env.local`** — a verificação do endpoint `/api/monitor` falhou localmente com 401. Rodar o secret novamente a partir do Vercel e atualizar `.env.local`.
3. **`SUPABASE_SERVICE_ROLE_KEY` desatualizada em `.env.local`** — bloqueou a verificação de saúde de execução DCA (não foi possível confirmar se há orders ativos nem a última execução). Atualizar a chave local a partir do Vercel/Supabase dashboard para restaurar esta verificação em runs futuros.

Nenhum destes itens indica uma falha em produção — são gaps de configuração local que impedem verificações completas. Recomenda-se sincronizar os secrets locais assim que possível.

## Persistência (commit-audit-report.mjs)

Falhou: `fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured`. O ambiente de execução deste run não tem a chave SSH de assinatura de commits configurada, pelo que o script não conseguiu criar o commit assinado no branch `audits/cadence`. Nenhuma tentativa adicional foi feita (sem retry, sem stash, sem force), conforme instrução. O relatório permanece apenas neste working copy até ser persistido manualmente ou num ambiente com a signing key configurada.

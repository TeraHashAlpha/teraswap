# TeraSwap Daily Health Report — 2026-07-09

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.98s |
| API Health | ✅ | HTTP 200, 0.53s |
| SSL Certificate | ✅ | Expires 2026-09-11 (~64 dias) |
| Monitor | ⚠️ | HTTP 401 — stale-local-secret (`MONITOR_SECRET` no `.env.local` desatualizado; não é outage de produção) |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH na Base (acima do limiar 0.01, margem ~15%) |
| DCA execution | ⚠️ | stale-local-key — Supabase devolve "Invalid API key" (`SUPABASE_SERVICE_ROLE_KEY` local desatualizado); saúde DCA não verificável |
| Git Activity (origin/main) | ℹ️ | 41 commits nas últimas 24h (PRs #277–#290: threat-model P1a/P2/P5a fixes, keeper hardening, DCA visibility+custom periods, API hardening, DefiLlama adapter) |
| KV / Rate Limiter | ✅ | Inferido saudável — API health HTTP 200 |

## Actions Taken

None required — nenhuma verificação de produção exigiu retry (site e API responderam 200 à primeira).

## Requires Attention

1. **🟡 `MONITOR_SECRET` local desatualizado** (repetido desde 2026-07-07) — `/api/monitor` devolve 401 com o segredo local. Cega a verificação de anomalias do monitor. Ação: atualizar `MONITOR_SECRET` no `.env.local` a partir do Vercel.
2. **🟡 `SUPABASE_SERVICE_ROLE_KEY` local desatualizado** (repetido desde 2026-07-07) — REST do Supabase devolve "Invalid API key". Cega a verificação de execuções DCA e ordens ativas. Ação: atualizar a key no `.env.local` a partir do dashboard do Supabase.
3. **🟡 Gás do keeper INALTERADO desde 2026-07-07** — saldo exatamente igual (0.01149 ETH, 5 casas decimais) ao relatório de 07-07, i.e. **zero gás gasto em ~2 dias**. Com a verificação Supabase cega (ponto 2), não é possível distinguir entre "sem ordens DCA ativas" (NORMAL) e "keeper parado com ordens pendentes" (seria CRITICAL). Recomenda-se verificação manual do keeper/ordens ativas assim que a key local for corrigida — isto eleva a urgência do ponto 2.
4. **ℹ️ Relatório de 2026-07-08 em falta** — não existe `health-2026-07-08.md` em `Audits/Daily/`; a corrida de ontem não terá sido executada/persistida.
5. **ℹ️ Gás próximo do limiar** — 0.01149 ETH está ~15% acima do WARNING (0.01). Preparar top-up.

**Resumo:** produção aparenta saudável (site, API, SSL, KV OK; 41 commits/24h em origin/main). WARNING mantém-se por dois segredos locais desatualizados que cegam as verificações 4 e 6 — agravado hoje pelo saldo de gás estático, que impede confirmar se o motor DCA está de facto a executar.

## PERSIST — erro (não repetido, conforme instrução)

`node scripts/commit-audit-report.mjs` falhou de novo, com a **mesma causa de 2026-07-07** — o relatório ficou escrito localmente mas **NÃO** foi commitado/pushed para `audits/cadence`:

```
warning: unable to unlink '.../.git/objects/ae/tmp_obj_3lrzhO': Operation not permitted
fatal: cannot lock ref 'HEAD': Unable to create
  '.../.git/worktrees/audit-cadence-7zkhiR/HEAD.lock': File exists.
'Another git process seems to be running in this repository...'
```

Diagnóstico: o sandbox não consegue apagar/criar locks em `.git/` (Operation not permitted) e existe um `HEAD.lock` órfão de worktrees `audit-cadence-*` de corridas anteriores falhadas. **Recorrente (2 corridas seguidas) → o passo PERSIST está estruturalmente quebrado neste ambiente.** Ação manual: limpar `.git/worktrees/audit-cadence-*` órfãos (`git worktree prune` + remover locks) e re-correr o script fora do sandbox; considerar ajustar o runbook/script para tolerar este ambiente.

# TeraSwap Daily Health Report — 2026-05-31

**Overall Status:** 🟡 WARNING

## Resultados

| Check | Status | Detalhes |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.93s (com redirect 307→200) |
| API Health | ⚠️ | HTTP 200, 3.34s — **acima do limiar de 3s** |
| SSL Certificate | ✅ | Expira 14 Jul 2026 (44 dias restantes) |
| Monitor | ⚠️ | `{"error":"Unauthorized"}` — MONITOR_SECRET não carregou do `.env` ou token dessincronizado |
| Git Activity | ℹ️ | 9 commits nas últimas 24h (Sprint 44/45 — Base swap prep + activation) |
| KV / Rate Limiter | ✅ | Inferido da disponibilidade do API Health (HTTP 200) |

## Notas

### API Health — Tempo de Resposta
O endpoint `/api/health` respondeu em **3.34s**, ligeiramente acima do limiar de 3s. Pode ser latência pontual do cold start serverless (Vercel Hobby). Monitorizar nos próximos dias — se persistir, pode indicar pressão no CPU (actualmente a 84% do plano Hobby).

### Monitor — Autenticação Falhou
O endpoint `/api/monitor` retornou `Unauthorized`. Causas possíveis:
1. `MONITOR_SECRET` não existe no `.env` local (apenas em Vercel env vars)
2. Token dessincronizado após rotação no incidente Vercel (ref: INC-2026-04-21)

Não é crítico — o monitor funciona via Cloudflare Worker cron em produção, não depende desta verificação local.

### Git Activity — Sprint 44/45
9 commits activos nas últimas 24h, todos relacionados com a preparação Base L2:
- P221–P227: chainId threading, router whitelist, FeeCollector per-chain, spender resolution, simulation, activation guard
- Consistente com o plano Sprint 44 (Base swap prep) e Sprint 45 (Base activation)

## Acções Tomadas
- Retry com `-L` (follow redirects) após HTTP 307 inicial nos endpoints site e API — ambos resolveram para HTTP 200.

## Requer Atenção
1. **API Health latência** (🟡 LOW) — Monitorizar tempo de resposta nos próximos 2-3 dias. Se consistentemente >3s, considerar upgrade para Vercel Pro (já planeado antes do marketing push).
2. **Monitor auth** (🟡 LOW) — Verificar se `MONITOR_SECRET` está no `.env` local. Em produção o cron do Cloudflare Worker usa a variável correcta, portanto sem risco operacional.

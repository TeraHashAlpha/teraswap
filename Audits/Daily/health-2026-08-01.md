# TeraSwap Daily Health Report — 2026-08-01

**Overall Status:** 🟡 WARNING

_Execução automatizada (scheduled task). Todos os checks read-only contra produção; o keeper **não** foi tocado._

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.637s |
| API Health | ✅ | HTTP 200, 0.428s — resposta `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira Sep 11 06:30:37 2026 GMT (~41 dias) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — `MONITOR_SECRET` local desactualizado. Gap de verificação **LOCAL**, não falha de produção |
| Keeper gas (0x71f5) | ⚠️ | 0.00736 ETH em Base — abaixo do limiar de aviso (0.01), acima do crítico (0.002) |
| DCA execution | ⚠️ | "Invalid API key" no Supabase — `SUPABASE_SERVICE_ROLE_KEY` local desactualizada. Check **cego** ao 4.º dia consecutivo |
| Git Activity (origin/main) | ℹ️ | 16 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido saudável via API Health (HTTP 200) |

## Detalhe — Monitor endpoint

O `/api/monitor` devolve HTTP 401 **também sem cabeçalho de autorização**, ou seja o endpoint está vivo e a
aplicar correctamente o auth gate — o 401 é rejeição de credencial, não indisponibilidade. Confirma a leitura
de que o problema é a cópia local do segredo, não produção.

## Detalhe — Git Activity (origin/main, últimas 24h)

HEAD de `origin/main`: `33a2e74` (2026-07-31 19:05:52 +0100). 16 commits, tema dominante = higiene de
dependências e reparação de CI/lint:

- `33a2e74` Merge PR #387 — `chore/lint-flat-config-repair`
- `0960d2e` fix(lint): repara registo de plugins do flat-config, retira `.eslintrc.json` morto
- `89a4f1c` Merge PR #386 — `chore/brace-expansion-override`
- `a9252a4` fix(deps): override de `brace-expansion` por major de `minimatch` (corrige quebra do eslint)
- `290f553` chore(deps): pin `brace-expansion` 5.0.8 — fecha GHSA-mh99-v99m-4gvg
- `1fadc3f` Merge PR #384 — `fix/codeql-version-sync`
- `b1da190` fix(ci): sincroniza pins do `codeql-action` para v4.37.4, agrupa bumps futuros de Actions
- `88b80d3` Merge PR #383 — `fix/keeper-redos-linearity`
- `7716e5a` fix(keeper): valida linearidade de `redactUrls` por rácio, não por ms absolutos
- `75216c2` / `b0b9de2` Dependabot — `@capacitor/core` 8.4.1 → 8.4.2
- `885719b` / `51efe69` Dependabot — grupo `dev-dependencies` (2 updates)

Nota positiva de segurança: `GHSA-mh99-v99m-4gvg` (brace-expansion) fechado por pin + override.

## Observação — keeper sem actividade on-chain (≥72h)

| Data | Saldo (Base) | Nonce |
|------|--------------|-------|
| 2026-07-29 | 0.00736 ETH | — |
| 2026-07-30 | 0.007356 ETH | — |
| 2026-07-31 | 0.00736 ETH | 14 |
| **2026-08-01** | **0.00736 ETH** | **14** |

Nonce **inalterado em 14** e saldo inalterado ⇒ **zero transacções do keeper nas últimas ~72h**. As duas
leituras continuam indistinguíveis nesta execução, por a chave Supabase local estar stale:

1. **NORMAL** — não existem ordens DCA activas com chunk vencido (cenário mais provável: Arbitrum ainda não
   flipado, actividade DCA em Base residual).
2. **WARNING** — keeper parado/stalling com ordens vencidas por executar.

Mantido a ⚠️ até o check Supabase ser restaurado. **Nenhuma acção correctiva tomada** — o keeper é read-only
por instrução.

## Actions Taken

Nenhuma. Site e API responderam HTTP 200 à primeira tentativa — sem retries. Nenhum processo reiniciado,
nenhum código alterado, nenhum commit além deste relatório.

## Requires Attention

1. 🟡 **`SUPABASE_SERVICE_ROLE_KEY` desactualizada em `.env.local` — 4.º dia consecutivo.** Enquanto não for
   actualizada, o check de saúde de execução DCA fica **cego**. Com o keeper a correr o motor DCA LIVE em
   Base e o nonce parado há 72h, este é de longe o gap mais relevante: é o único check que distinguiria
   "sem ordens vencidas" de "keeper stalled". Actualizar a partir do Supabase dashboard.
2. 🟡 **Keeper gas baixo (0.00736 ETH em Base)** — abaixo do limiar de aviso há ≥4 dias. Reabastecer
   `0x71f5AC191587AE132D966a719569b2468e0Aa2E5`. Sem consumo actual não há risco imediato, mas o saldo não
   cobre um burst de execuções.
3. 🟡 **`MONITOR_SECRET` desactualizado em `.env.local`** — actualizar com o valor do Vercel para restaurar a
   verificação local de `/api/monitor`.

Nota de processo: os pontos 1 e 3 repetem-se há 4 dias. Sugere-se um passo único de re-sync de `.env.local`
a partir do Vercel/Supabase (`vercel env pull`), que fecha ambos de uma vez.

## Erro de Persistência (`scripts/commit-audit-report.mjs`)

O script falhou neste sandbox (sem signing key configurada), tal como em 2026-07-31:

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
```

Sem retry, sem stash, sem bypass de assinatura — conforme instrução (regra #12: nunca commitar sem
assinatura). Este relatório (`Audits/Daily/health-2026-08-01.md`) fica **por persistir** na branch
`audits/cadence`; requer commit manual ou execução num ambiente com signing key. A falha é específica do
sandbox — o GitHub Action de cadência persiste normalmente.

Pendente de persistência: `health-2026-07-31.md` e `health-2026-08-01.md`.

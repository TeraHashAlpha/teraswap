# TeraSwap Daily Health Report — 2026-07-31

**Overall Status:** 🟡 WARNING

_Execução automatizada (scheduled task). Checks read-only contra produção; o keeper não foi tocado._

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.574s |
| API Health | ✅ | HTTP 200, 0.377s — resposta `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira Sep 11 06:30:37 2026 GMT (~42 dias) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — `MONITOR_SECRET` local desatualizado. Gap de verificação LOCAL, **não** falha de produção (o run do GitHub Action de 2026-07-30 obteve HTTP 200 no mesmo endpoint) |
| Keeper gas (0x71f5) | ⚠️ | 0.00736 ETH em Base — abaixo do limiar de aviso (0.01 ETH), acima do crítico (0.002 ETH) |
| DCA execution | ⚠️ | "Invalid API key" no Supabase — `SUPABASE_SERVICE_ROLE_KEY` local desatualizada. Gap de verificação LOCAL, não falha do keeper. Não foi possível ler últimas execuções nem ordens DCA activas |
| Git Activity (origin/main) | ℹ️ | 6 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido saudável via API Health (HTTP 200) |

## Detalhe — Git Activity (origin/main, últimas 24h)

- `1d29a4f` Merge PR #380 — `test/dca-arbitrum-activation-switch`
- `73bee93` test(dca): prova do switch de activação Arbitrum antes do flip
- `05f5a1a` Merge PR #379 — `fix/dca-panel-oracle-fail-closed`
- `6508615` Merge branch 'main' into `fix/dca-panel-oracle-fail-closed`
- `a3cebb8` fix(oracle): fecha findings de auditoria L-1/L-2 no gate de oráculo do painel DCA
- `40e8ea6` Merge PR #373 — `chore/ci-full-suite-job`

## Observação — keeper sem actividade on-chain

O saldo do executor 0x71f5AC191587AE132D966a719569b2468e0Aa2E5 está **inalterado** face ao relatório de
2026-07-30 (0.007356 ETH) e de 2026-07-29 (0.00736 ETH); nonce actual em Base = **14**. Ou seja, não houve
qualquer transacção do keeper nas últimas ~48h.

Duas leituras possíveis, **não distinguíveis** nesta execução por a chave Supabase local estar stale:

1. **NORMAL** — não existem ordens DCA activas com chunk vencido (cenário mais provável: Arbitrum ainda não
   flipado, actividade DCA em Base residual).
2. **WARNING** — keeper parado/stalling com ordens vencidas por executar.

Classificado como ⚠️ até o check Supabase ser restaurado. **Nenhuma acção correctiva tomada** (keeper é
read-only por instrução).

## Actions Taken
Nenhuma. Site e API responderam HTTP 200 à primeira tentativa — sem retries. Nenhum processo reiniciado,
nenhum código alterado, nenhum commit além deste relatório.

## Requires Attention

1. **`SUPABASE_SERVICE_ROLE_KEY` desactualizada em `.env.local`** (3.º dia consecutivo) — enquanto não for
   actualizada, o check de saúde de execução DCA fica **cego**. Com o keeper a correr o motor DCA LIVE em
   Base, este é o gap mais relevante da lista. Actualizar a partir do Supabase dashboard.
2. **Keeper gas baixo (0.00736 ETH em Base)** — abaixo do limiar de aviso há ≥3 dias. Reabastecer
   0x71f5AC191587AE132D966a719569b2468e0Aa2E5 antes de atingir o piso crítico (0.002 ETH).
3. **`MONITOR_SECRET` desactualizado em `.env.local`** — actualizar com o valor do Vercel para restaurar a
   verificação local de `/api/monitor` (produção confirmada saudável por via independente).

## Erro de Persistência (`scripts/commit-audit-report.mjs`)

O script falhou neste sandbox (sem signing key configurada):

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
```

Sem retry, sem stash, sem bypass de assinatura — conforme instrução. Este relatório
(`Audits/Daily/health-2026-07-31.md`) fica **por persistir** na branch `audits/cadence`; requer commit manual
ou execução num ambiente com signing key. Nota: o relatório de 2026-07-30 foi persistido com sucesso pelo
GitHub Action (`3619294` em `origin/audits/cadence`), pelo que a falha é específica deste sandbox.

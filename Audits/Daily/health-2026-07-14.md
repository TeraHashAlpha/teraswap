# TeraSwap Daily Health Report — 2026-07-14

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.64s |
| API Health | ✅ | HTTP 200, 0.36s |
| SSL Certificate | ✅ | Expires 2026-09-11 (~59 days) |
| Monitor | ⚠️ | HTTP 401 — stale-local-secret (`MONITOR_SECRET` em `.env.local`, 5.º dia) |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH on Base (inalterado desde 2026-07-11 — zero gas gasto) |
| DCA execution | ⚠️ | stale-local-key (Supabase) — mas evidência on-chain: última execução **2026-07-05 11:53 UTC** (ver nota) |
| Git Activity (origin/main) | ℹ️ | 1 commit em 24h: `aebfed0` Merge PR #305 (`chore/hygiene-2`) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health 200 |

## Actions Taken
- Nenhum retry necessário — site e API responderam 200 à primeira. Keeper não tocado (read-only).
- **Fallback read-only para o check DCA:** com a `SUPABASE_SERVICE_ROLE_KEY` local stale (5.º dia cego), consultei o histórico on-chain do executor (0x71f5) via Blockscout Base (API pública, sem chave). Resultado: execuções diárias com sucesso a ~11:52 UTC de **2026-07-01 a 2026-07-05**, e **nenhuma transação desde 2026-07-05** (9 dias). O padrão — cadência diária limpa, 5 fills consecutivos, todos `ok`, paragem sem falhas — é consistente com uma ordem DCA diária que **completou** normalmente a 05-07. Isto também explica o gas inalterado desde 11-07 observado nos relatórios anteriores.
- ⚠️ Contudo, sem acesso ao Supabase **não é possível confirmar** que não existem ordens ativas em atraso. Se existir alguma ordem DCA ativa criada depois de 05-07, o keeper estaria parado há dias → seria CRITICAL. Classificado como WARNING (cenário mais provável = conclusão normal), pendente de confirmação.

## Requires Attention
1. 🟡 **Atualizar `MONITOR_SECRET` em `.env.local`** — check do monitor cego pelo 5.º dia consecutivo.
2. 🟡 **Atualizar `SUPABASE_SERVICE_ROLE_KEY` em `.env.local`** — 5.º dia cego. É o único passo que resolve a ambiguidade "DCA completou" vs "keeper parado com ordens pendentes". **Prioridade máxima destes três.**
3. ℹ️ **Gas do keeper = 0.01149 ETH** — apenas 0.0015 acima do threshold WARNING (0.01). Planear top-up antes da próxima ordem DCA ativa.
4. 🟡 **Persistência dos relatórios** — ver secção abaixo; relatórios de 2026-07-09 em diante por persistir no branch `audits/cadence` se o script voltar a falhar (SSH signing key indisponível no sandbox nas execuções anteriores).

## Persist Error (commit-audit-report.mjs)

Mesma causa dos dias anteriores (6.º dia) — SSH signing key não configurada/acessível no sandbox:

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
(+ warnings "unable to unlink .git/objects/**/tmp_obj_*: Operation not permitted")
```

Há agora **6 relatórios por persistir** (2026-07-09 a 2026-07-14) no branch `audits/cadence` — correr o script manualmente numa sessão com a SSH signing key disponível. Não foi feito retry, stash nem force, conforme convenção.

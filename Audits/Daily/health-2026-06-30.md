# TeraSwap Daily Health Report — 2026-06-30

**Overall Status:** 🟡 WARNING

_Execução automática (UTC 07:07). Verificação read-only — nenhum processo reiniciado, nenhum commit, nenhuma alteração de código._

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 1.05s |
| API Health | ✅ | HTTP 200, 0.91s |
| SSL Certificate | ✅ | Expira Sep 11 2026 (~73 dias) |
| Monitor | ⚠️ | `{"error":"Unauthorized"}` — segredo local parece desatualizado |
| Keeper gas (0x71f5) | ⚠️ | 0.00706 ETH na Base (< 0.01 WARNING; > 0.002 CRITICAL) |
| DCA execution | ⚠️ | Lacuna de verificação — `SUPABASE_SERVICE_ROLE_KEY` local inválida (keeper vivo via saldo de gás) |
| Git Activity (origin/main) | ℹ️ | 20 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | API 200 — KV operacional |

## Detalhe

**Site / API / SSL / KV** — Tudo OK. Tempos de resposta saudáveis (~1s). Certificado SSL válido por ~73 dias (sem ação). KV/rate limiter inferido como saudável a partir da disponibilidade da API.

**Keeper gas (0x71f5AC19…0Aa2E5) — 🟡 WARNING.** Saldo na Base = **0.00706 ETH**, abaixo do limiar de aviso (0.01 ETH) mas acima do crítico (0.002 ETH). O keeper ainda consegue pagar gás, mas convém **reabastecer** para evitar que execuções DCA estagnem silenciosamente. Read-only — não foi feita qualquer transação.

**Monitor — 🟡 WARNING.** O endpoint `/api/monitor` responde mas devolve `Unauthorized` tanto com `MONITOR_SECRET` como com `MONITOR_CRON_SECRET` do `.env.local`. O endpoint está vivo (o site responde 200); o mais provável é que o segredo tenha sido rodado em produção (Vercel) e os ficheiros `.env`/`.env.local` locais estejam desatualizados. Sem dados do monitor, não foi possível inspecionar taxas de erro / eventos de segurança nesta execução.

**DCA execution — 🟡 lacuna de verificação.** O script da tarefa usa `$SUPABASE_URL`, que **não existe** nos ficheiros env (a variável real é `NEXT_PUBLIC_SUPABASE_URL`). Reexecutei com o URL correto, mas a `SUPABASE_SERVICE_ROLE_KEY` local devolve `Invalid API key` — chave provavelmente rodada/desatualizada. Não foi possível ler `order_executions` nem `orders` ativas. **Não é uma falha do keeper:** o saldo de gás (0.00706 ETH) confirma que a wallet do keeper está viva e fundeada. Mantém-se apenas a impossibilidade de confirmar a cadência de execução por esta via.

**Git (origin/main) — ℹ️.** `git fetch` OK. **20 commits** nas últimas 24h, incluindo merges #244 (wallet-logos / CSP `blob:` em img-src), #243/#242/#240 (mobile-ux polish), #236 (DCA Positions dashboard), e várias bumps Dependabot (next 16.2.9, @sentry/nextjs 10.59.0, @capacitor/cli 8.4.1, esbuild). Atividade normal de desenvolvimento.

## Actions Taken

Nenhuma alteração ao sistema. Ações de diagnóstico read-only:

- Reexecutei o check do Monitor com `MONITOR_CRON_SECRET` como alternativa (também `Unauthorized`).
- Reexecutei o check Supabase com `NEXT_PUBLIC_SUPABASE_URL` (URL correto) — chave de serviço inválida.
- `git fetch origin` antes de ler `origin/main` (origin/main confirmado, não o checkout local).

## Requires Attention

1. **🟡 Reabastecer gás do keeper** — 0x71f5AC19…0Aa2E5 na Base está em **0.00706 ETH**. Adicionar ETH na Base antes de cair abaixo de 0.002 (estagnação de execuções DCA). *Ação manual do TeraHash — não executar transações automaticamente.*
2. **🟡 Segredos locais desatualizados (monitoramento degradado)** — tanto o segredo do Monitor (`MONITOR_SECRET`/`MONITOR_CRON_SECRET`) como a `SUPABASE_SERVICE_ROLE_KEY` em `.env.local` são rejeitados pelos serviços. Atualizar os valores locais a partir do Vercel/Supabase para restaurar a cobertura de monitorização diária (Monitor + verificação de execuções DCA). Sem isto, estes dois checks continuarão cegos.
3. **🟢 Nota para a tarefa agendada** — o `SKILL.md` referencia `$SUPABASE_URL`, mas a variável correta no repo é `NEXT_PUBLIC_SUPABASE_URL`. Corrigir o nome no script da tarefa evita a falsa "lacuna de verificação" nas próximas execuções.

_Sem itens CRITICAL. Site, API, SSL e KV totalmente operacionais._

# TeraSwap Daily Health Report — 2026-07-05

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.58s |
| API Health | ✅ | HTTP 200, 0.50s |
| SSL Certificate | ✅ | Expira 11 Set 2026 (68 dias) |
| Monitor | ⚠️ | HTTP 401 — segredo local `MONITOR_SECRET` desatualizado (rodado no Vercel, não localmente). Não é falha de produção. |
| Keeper gas (0x71f5) | ✅ | 0.01199 ETH em Base (acima do limiar 0.01, mas próximo — vigiar) |
| DCA execution | ⚠️ | `Invalid API key` — `SUPABASE_SERVICE_ROLE_KEY` local desatualizada. Verificação cega localmente; não é falha do keeper (`NEXT_PUBLIC_SUPABASE_URL` presente e correto). |
| Git Activity (origin/main) | ℹ️ | 0 commits nas últimas 24h (fetch de origin/main OK) |
| KV / Rate Limiter | ✅ | HTTP 200 (inferido de /api/health) |

## Actions Taken
Nenhuma ação de escrita executada (execução read-only, conforme mandato). Sem retries necessários — site e API responderam HTTP 200 à primeira tentativa. Keeper NÃO tocado (monitorização read-only apenas).

## Requires Attention

Dois segredos locais desatualizados estão a cegar verificações no ambiente de execução (não afetam produção):

1. **`MONITOR_SECRET`** (`.env.local`) — rodado no Vercel mas não atualizado localmente → check do Monitor devolve 401. Atualizar a variável local a partir do Vercel para restaurar a visibilidade do endpoint `/api/monitor`.
2. **`SUPABASE_SERVICE_ROLE_KEY`** (`.env.local`) — desatualizada → `Invalid API key` no Supabase, impedindo a verificação da saúde de execução DCA. Atualizar a partir do painel Supabase para voltar a monitorizar execuções DCA e ordens ativas.

**Nota operacional:** o saldo de gás do keeper (0.01199 ETH em Base) está apenas ~0.002 ETH acima do limiar de WARNING (0.01 ETH). Não é crítico hoje, mas recomenda-se um top-up preventivo da wallet executora `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` para evitar que os próximos dias cruzem o limiar e ativem alerta.

Enquanto a chave Supabase local não for atualizada, a saúde de execução DCA não pode ser confirmada por esta verificação diária — o saldo de gás do keeper (positivo) é o único sinal indireto de que o motor de ordens permanece financiado e operacional.

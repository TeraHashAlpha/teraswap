# TeraSwap Daily Health Report — 2026-07-18

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.56s |
| API Health | ✅ | HTTP 200, 0.36s |
| SSL Certificate | ✅ | Expires 2026-09-11 (~55 dias) |
| Monitor | ⚠️ | HTTP 401 — stale-local-secret (`MONITOR_SECRET` local desatualizado; NÃO é outage de produção) |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH on Base (acima do limiar 0.01, mas com pouca margem) |
| DCA execution | ⚠️ | stale-local-key — Supabase devolveu "Invalid API key" (`SUPABASE_SERVICE_ROLE_KEY` local desatualizada); estado do keeper NÃO verificável via DB neste run |
| Git Activity (origin/main) | ℹ️ | 2 commits nas últimas 24h — #309 merge `chore/arbitrum-ui-polish` (b09ae2c) + fix Arbitrum UI polish (c37939b) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health (HTTP 200) |

## Actions Taken
None required — todos os endpoints responderam à primeira tentativa; sem retries.

## Requires Attention
1. 🟡 **Segredos locais desatualizados (blind spots nos checks):** atualizar em `.env.local` o `MONITOR_SECRET` e a `SUPABASE_SERVICE_ROLE_KEY` (foram rodados na Vercel/Supabase e não localmente). Enquanto não forem atualizados, os checks de Monitor e DCA execution ficam cegos — hoje não foi possível confirmar via DB se o keeper está a executar chunks DCA.
2. 🟡 **Gas do keeper perto do limiar:** 0.01149 ETH na Base — apenas ~15% acima do limiar de WARNING (0.01). Recomenda-se top-up preventivo do executor 0x71f5…A2E5 nos próximos dias.

## Persist (commit-audit-report.mjs)
❌ Falhou no ambiente sandbox — não retentado (conforme instruções):

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
(status 128; + warnings "unable to unlink .git/objects/*/tmp_obj_*: Operation not permitted")
```

A sessão não tem acesso à chave de assinatura SSH (regra: commits sempre assinados). Passo manual: correr `node scripts/commit-audit-report.mjs` numa sessão com a signing key configurada.

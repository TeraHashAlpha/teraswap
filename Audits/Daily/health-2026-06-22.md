# TeraSwap Daily Health Report — 2026-06-22

**Overall Status:** 🟡 WARNING

> Execução automática do bot de auditoria (read-only). Hora UTC: 2026-06-22T07:07Z.

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 308 → 200 (redirect apex → www.teraswap.app), 0.50–1.04s. Site operacional. |
| API Health | ✅ | HTTP 308 → 200, 0.94s. Corpo: `{"status":"OK"}`. |
| SSL Certificate | ✅ | Expira Sep 11 2026 06:30 GMT (81 dias restantes). |
| Monitor | ⚠️ | `/api/monitor` devolveu `{"error":"Unauthorized"}` (HTTP 401) com ambos os segredos locais (`.env` 64 chars e `.env.local` 66 chars). 2.º dia consecutivo. |
| Git Activity | ℹ️ | 0 commits nas últimas 24h. |
| KV / Rate Limiter | ✅ | Inferido saudável a partir da disponibilidade da API (health = HTTP 200). Diagnóstico detalhado de KV requer `?token=HEALTH_TOKEN`, não consultado. |

## Detalhe das verificações

**1–2. Disponibilidade e tempo de resposta.** O domínio apex `teraswap.app` responde com HTTP 308 (redirect permanente) para `https://www.teraswap.app/`. Seguindo o redirect, tanto o site como `/api/health` resolvem para HTTP 200, com tempos de resposta bem abaixo do limiar de 3s (0.50s–1.04s). Não é uma falha — é a configuração normal de redirect apex→www. O site está operacional.

**3. Certificado SSL.** Válido até 11 de setembro de 2026 (06:30 GMT). Restam 81 dias, muito acima do limiar de WARNING (14 dias). Sem ação necessária.

**4. Monitor endpoint.** O endpoint `/api/monitor` rejeitou o pedido com `{"error":"Unauthorized"}` (HTTP 401). Foram testados **ambos** os segredos locais: o de `.env` (comprimento 64) e o de `.env.local` (comprimento 66, com aspas), e ambos falharam a autenticação. O endpoint está acessível e responde de forma limpa (JSON 401, não 5xx nem timeout), pelo que o serviço em si está de pé — o problema é exclusivamente de credencial. Causa provável: o `MONITOR_SECRET` foi rodado/alterado em produção (Vercel) e os ficheiros `.env` locais estão desatualizados. Classificado como WARNING conforme protocolo (não crítico). Como o segredo não autenticou, não foi possível ler métricas de erro nem eventos de segurança nesta execução. **Nota:** mesma falha registada no relatório de 2026-06-21 — 2.º dia consecutivo sem reconciliação do segredo.

**5. Atividade Git (24h).** Nenhum commit no repositório nas últimas 24 horas. Último commit: `df00d35` (2026-06-17) — "audit: SPRINT-201 DCA observability/freeze APPROVED (0C/0H/0M/1L/6I)". Coerente com Sprint 9B em pausa (P68 mainnet deploy pendente).

**6. KV / Rate Limiter.** O `/api/health` básico respondeu 200 sem expor estado de KV (requer `?token=HEALTH_TOKEN`). Saúde do KV inferida da disponibilidade da API. Sem indícios de problema.

## Actions Taken

- Seguido o redirect 308→200 para confirmar que site e API estão efetivamente UP. Como o primeiro follow já devolveu HTTP 200, **não foram necessárias as 3 retentativas** do protocolo (o 308 é um redirect permanente intencional, não uma falha transitória).
- Testados ambos os segredos locais (`.env` e `.env.local`) contra `/api/monitor` para distinguir credencial em falta de credencial inválida — concluiu-se que é inválida/desatualizada (ambos retornaram 401). Valores não expostos (apenas comprimento).
- Comparado com o relatório do dia anterior (2026-06-21) para confirmar recorrência do problema do monitor.
- Nenhum processo reiniciado, nenhum ficheiro de código alterado, nenhum commit efetuado (bot read-only). O executor permanece intencionalmente parado (L2 pendente) — não verificado nem reiniciado.

## Requires Attention

- **Monitor endpoint (⚠️, recorrente — 2.º dia):** reconciliar o `MONITOR_SECRET` local com o valor em produção (Vercel env vars). A falha repete-se desde 2026-06-21, o que reforça a hipótese de rotação de segredo não propagada para o ambiente local. Até resolver, as métricas de erro e eventos de segurança expostos por `/api/monitor` **não são consultáveis por este bot**, criando um ponto cego na monitorização de segurança. Recomenda-se: (1) confirmar no painel Vercel o valor atual de `MONITOR_SECRET`; (2) atualizar `.env`/`.env.local`; (3) re-executar este check para confirmar HTTP 200.

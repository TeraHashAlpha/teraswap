# TeraSwap Daily Health Report — 2026-04-23

**Overall Status:** 🟡 WARNING

## Resultados

| Check | Status | Detalhes |
|-------|--------|----------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.555s (via 307→www) |
| API Health | ✅ | HTTP 200, 1.496s (via 307→www) |
| SSL Certificate | ✅ | Expira 2026-07-14 (81 dias restantes) |
| Monitor | ⚠️ | Unauthorized — provável dessincronização do MONITOR_SECRET local vs. Vercel |
| Git Activity | ℹ️ | 1 commit nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido via disponibilidade do API Health (HTTP 200) |

## Detalhes

### Site & API
Ambos os endpoints respondem com HTTP 200 após redirect 307 (teraswap.app → www.teraswap.app). Tempos de resposta dentro dos limites normais (site: 0.55s, API: 1.50s).

### Certificado SSL
Válido até 14 de julho de 2026 — 81 dias restantes. Sem ação necessária.

### Monitor Endpoint
O endpoint `/api/monitor` retorna `{"error":"Unauthorized"}` com Bearer token do `.env` local (64 caracteres). A autenticação usa `safeCompare` contra `process.env.MONITOR_SECRET` no servidor. **Causa provável:** após a rotação de secrets do incidente Vercel (2026-04-21), o `MONITOR_SECRET` no Vercel pode ter sido atualizado sem sincronizar o `.env` local. O comentário no código (linha 38) refere `?key=` query param mas a implementação real usa `Authorization: Bearer` header — comentário desatualizado mas sem impacto funcional.

### Git Activity (últimas 24h)
1 commit:
- `de9e097` — feat(chainlink): expand oracle registry with PAXG, stETH, FRAX, LUSD, GRT

### KV / Rate Limiter
O health endpoint não expõe estado KV explicitamente. Saúde inferida pela disponibilidade do API (HTTP 200).

## Actions Taken
- Retry não necessário — site e API responderam com sucesso na primeira tentativa (após follow redirect).
- Tentativas múltiplas de autenticação no monitor (Bearer header, query param, x-monitor-secret header) — todas falharam.

## Requires Attention
1. **🟡 MONITOR_SECRET dessincronizado** — O `.env` local tem um `MONITOR_SECRET` de 64 caracteres que o endpoint deployed rejeita. Verificar no Vercel Dashboard → Settings → Environment Variables se o valor foi alterado durante a rotação pós-breach (2026-04-21). Sincronizar o `.env` local com o valor actual do Vercel.
2. **ℹ️ Comentário desatualizado** — `src/app/api/monitor/route.ts` linha 38 refere autenticação via `?key=` query param, mas a implementação (linhas 41-49) usa `Authorization: Bearer`. Baixa prioridade, mas pode confundir futuros operadores.

# TeraSwap Daily Health Report — 2026-07-27

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.40s |
| API Health | ✅ | HTTP 200, 0.35s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~46 dias) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — `MONITOR_SECRET` local desatualizado (rotacionado na Vercel, não no `.env.local`); gap de verificação LOCAL, não outage de produção |
| Keeper gas (0x71f5) | ⚠️ | 0.00736 ETH em Base — abaixo do limiar de 0.01 ETH (WARNING); acima do crítico de 0.002 ETH |
| DCA execution | ⚠️ | Não verificável — `SUPABASE_SERVICE_ROLE_KEY` local inválida ("Invalid API key"); gap de verificação LOCAL, não falha do keeper |
| Git Activity (origin/main) | ℹ️ | 0 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido normal via API Health (HTTP 200) |

## Actions Taken
Nenhuma ação de escrita executada (check read-only). Sem retries necessários — site e API responderam 200 à primeira tentativa.

## Requires Attention
- **Keeper gas baixo (0x71f5AC19...):** 0.00736 ETH em Base, abaixo do limiar de aviso (0.01 ETH). Recomenda-se reabastecer a wallet do keeper antes de atingir o limiar crítico (0.002 ETH), sob pena de o keeper não conseguir pagar gas para executar chunks de DCA.
- **`MONITOR_SECRET` desatualizado em `.env.local`:** rotacionado na Vercel mas não sincronizado localmente — impede verificação do endpoint `/api/monitor` a partir daqui. Atualizar o valor local.
- **`SUPABASE_SERVICE_ROLE_KEY` inválida em `.env.local`:** impede verificação do estado de execução das ordens DCA (últimas execuções e ordens ativas) a partir daqui. Atualizar o valor local para restaurar visibilidade sobre a saúde do keeper.
- Ambos os gaps acima são de verificação **local** — não há evidência de outage em produção; requerem apenas sincronização de segredos, não intervenção de emergência.

## Persist Error

`node scripts/commit-audit-report.mjs` falhou (exit 1): `fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured` no worktree temporário de commit. Também identificou 2 relatórios anteriores (health-2026-07-25.md, health-2026-07-26.md) ainda por persistir. Conforme instrução da task, não foi feito retry, stash ou bypass — reportado apenas.


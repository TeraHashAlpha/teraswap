# TeraSwap Daily Health Report — 2026-07-21

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.314s |
| API Health | ✅ | HTTP 200, 0.367s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~52 dias) |
| Monitor | ⚠️ | 401 Unauthorized → stale-local-secret (`MONITOR_SECRET` em `.env.local` desatualizado; NÃO é outage de produção) |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH on Base (acima do limiar WARNING de 0.01, mas por pouca margem — monitorizar) |
| DCA execution | ⚠️ | "Invalid API key" → stale-local-key (`SUPABASE_SERVICE_ROLE_KEY` desatualizado localmente); estado do keeper NÃO verificável hoje — não é falha do keeper |
| Git Activity (origin/main) | ℹ️ | 0 commits nas últimas 24h (fetch OK; tip = `18695c5` Merge PR #311, 2026-07-19 18:32) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health HTTP 200 |

## Actions Taken
None required — todos os endpoints responderam à primeira tentativa; sem retries.

## Requires Attention
1. 🟡 **Atualizar `MONITOR_SECRET` em `.env.local`** — o secret local está desatualizado (rodado na Vercel), deixando o check do monitor cego.
2. 🟡 **Atualizar `SUPABASE_SERVICE_ROLE_KEY` em `.env.local`** — chave local inválida; o check de execução DCA está cego. Enquanto ambas as chaves não forem atualizadas, este relatório não consegue confirmar a saúde do keeper via Supabase.
3. ℹ️ **Gas do keeper perto do limiar** — 0.01149 ETH vs limiar WARNING 0.01 ETH. Considerar top-up preventivo do executor 0x71f5 na Base.

Nota: warnings `unable to unlink .git/objects/*` durante o fetch (permissões do sandbox) — fetch concluído com sucesso (FETCH_HEAD atualizado 2026-07-21 08:06).

## Persist Error
`node scripts/commit-audit-report.mjs` falhou (não retentado, conforme runbook):

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
```

O ambiente sandbox não tem a signing key SSH configurada (Rule #12 — commits assinados obrigatórios), pelo que o commit para `audits/cadence` não foi efetuado. Passo manual: correr o script a partir da máquina do owner, ou commitar o relatório manualmente com assinatura.

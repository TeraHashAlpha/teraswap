# TeraSwap Daily Health Report — 2026-07-11

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.62s |
| API Health | ✅ | HTTP 200, 0.43s |
| SSL Certificate | ✅ | Expires 2026-09-11 (62 dias) |
| Monitor | ⚠️ | HTTP 401 — stale-local-secret (`MONITOR_SECRET` local desatualizado; rodado no Vercel, não é outage de produção) |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH on Base (nota: apenas ~15% acima do limiar de WARNING 0.01 — top-up em breve recomendado) |
| DCA execution | ⚠️ | stale-local-key — Supabase devolve "Invalid API key" (`SUPABASE_SERVICE_ROLE_KEY` local desatualizado); estado do keeper NÃO verificável hoje |
| Git Activity (origin/main) | ℹ️ | 5 commits nas últimas 24h — merge #304 (chore/47c-arbitrum-catalog: catálogo Arbitrum 42161 a partir do manifest + guard estendido + runbook) e merge #303 (sprint/47-arbitrum-activation-prep) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health (HTTP 200) |

## Actions Taken
None required — nenhum retry necessário (site e API responderam 200 à primeira). Keeper não tocado (read-only).

## Requires Attention
1. 🟡 **Atualizar `MONITOR_SECRET` em `.env.local`** — o secret local está desatualizado face ao Vercel; o check do monitor está cego até ser sincronizado.
2. 🟡 **Atualizar `SUPABASE_SERVICE_ROLE_KEY` em `.env.local`** — chave local inválida; a verificação de execuções DCA e ordens ativas está cega. Com DCA LIVE na Base, este é o check mais importante a restaurar.
3. ℹ️ **Gas do keeper (0x71f5) a 0.01149 ETH** — acima do limiar mas com pouca margem; planear top-up para evitar WARNING nos próximos dias.
4. 🟡 **Persistência dos relatórios bloqueada** — `scripts/commit-audit-report.mjs` falhou (ver erro abaixo). Há **3 relatórios por persistir** (2026-07-09, 2026-07-10, 2026-07-11) no branch `audits/cadence` — correr o script manualmente numa sessão com a signing key disponível.

## Persist Error (commit-audit-report.mjs)

O script identificou 3 report(s) por persistir mas falhou no commit — a signing key SSH não está configurada/acessível neste ambiente (sandbox):

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
(+ warnings "unable to unlink .git/objects/**/tmp_obj_*: Operation not permitted")
```

Não foi feito retry, stash nem force, conforme convenção.

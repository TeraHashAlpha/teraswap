# TeraSwap Daily Health Report — 2026-04-28

**Overall Status:** 🟢 NORMAL

## Resultados

| Check | Status | Detalhes |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.58s resposta (redirect 307 → 200) |
| API Health | ✅ | HTTP 200, 1.70s resposta (redirect 307 → 200) |
| SSL Certificate | ✅ | Expira 14 Jul 2026 (77 dias restantes) |
| Monitor | ⚠️ | Endpoint retornou `{"error":"Unauthorized"}` — MONITOR_SECRET possivelmente ausente do `.env` local ou dessincronizado |
| Git Activity | ℹ️ | 1 commit nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido a partir da disponibilidade do API Health (HTTP 200) |

## Detalhes dos Checks

### Site & API
Ambos os endpoints respondem com HTTP 307 (redirect) seguido de HTTP 200. Tempos de resposta dentro dos limites normais (< 3s). Sem anomalias.

### SSL Certificate
Certificado válido até 14 de Julho de 2026 — 77 dias restantes. Nenhuma ação necessária.

### Monitor Endpoint
O endpoint `/api/monitor` retornou `{"error":"Unauthorized"}`. Causas prováveis:
1. Variável `MONITOR_SECRET` não definida no `.env` local usado pelo health check
2. Secret dessincronizado entre ambiente local e Vercel

**Nota:** Isto não indica um problema em produção — o monitor continua funcional via Cloudflare Worker cron com o secret correto. A falha é apenas na autenticação do check local.

### Git Activity (últimas 24h)
1 commit:
- `75d6cb7` — fix(monitor): add Supabase keepalive ping to prevent free-tier pause

### KV / Rate Limiter
O endpoint de health não expõe estado do KV diretamente. Saúde do KV inferida a partir da resposta bem-sucedida do API Health (HTTP 200).

## Ações Tomadas
Nenhuma ação necessária. Todos os checks principais passaram.

## Requer Atenção
- **Monitor auth (LOW):** Verificar se `MONITOR_SECRET` no `.env` local está sincronizado com o valor em produção (Vercel). Não afeta produção — apenas este health check local.

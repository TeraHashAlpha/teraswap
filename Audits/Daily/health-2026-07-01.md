# TeraSwap Daily Health Report — 2026-07-01

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.656s |
| API Health | ✅ | HTTP 200, 0.612s |
| SSL Certificate | ✅ | Expira 2026-09-11 (72 dias) |
| Monitor | ⚠️ | HTTP 401 — segredo local desatualizado (stale-local-secret) |
| Keeper gas (0x71f5) | ⚠️ | 0.00706 ETH na Base (< 0.01 ETH) |
| DCA execution | ⚠️ | "Invalid API key" — chave Supabase local desatualizada (stale-local-key) |
| Git Activity (origin/main) | ℹ️ | 7 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | HTTP 200 (API disponível) |

## Detalhe

**Produção (sistemas em linha):** o site, a API `/api/health` e o rate limiter/KV respondem todos HTTP 200 com tempos de resposta saudáveis (< 0.7s). Certificado SSL válido por mais 72 dias. Nenhum indício de outage.

**Keeper — gas na Base (⚠️):** a carteira executora `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` tem **0.00706 ETH** na Base, abaixo do limiar de aviso de 0.01 ETH (acima do crítico de 0.002 ETH). O keeper continua operacional e capaz de pagar gas, mas o saldo deve ser reforçado em breve para evitar que atinja o limiar crítico e falhe a execução de chunks DCA. **Ação read-only apenas — não reiniciar nem modificar o keeper.**

**Segredos locais desatualizados (⚠️ — lacuna de verificação local, NÃO outage de produção):**
- `/api/monitor` devolveu HTTP 401 (Unauthorized) → o `MONITOR_SECRET` em `.env.local` está desatualizado (provavelmente rodado no Vercel e não sincronizado localmente).
- As queries ao Supabase devolveram `Invalid API key` → o `SUPABASE_SERVICE_ROLE_KEY` em `.env.local` está desatualizado.

Consequência: as verificações 4 (Monitor) e 6 (DCA execution health) ficaram **cegas nesta execução**. Não foi possível confirmar a última execução DCA nem listar ordens DCA ativas via Supabase. Isto NÃO indica falha do keeper — o saldo de gas confirma que a carteira executora está ativa. É uma lacuna de instrumentação local, corrigível ao atualizar `.env.local`.

**Git (origin/main, últimas 24h):** 7 commits, com destaque para o merge da resiliência DCA (#246) e o email de suporte unificado (#245):
- `88c04c0` Merge PR #246 — chore/dca-resilience
- `04c2b87` Merge branch 'main' into chore/dca-resilience
- `184e83a` ci(dca-resilience): gate DCA-resilience UI regressions + FEEDBACK
- `6dfc3d3` feat(dca-ui): surface the real failure reason + guard impossible schedules
- `cb40a34` fix(keeper): don't permanently fail a routable DCA on a transient miss
- `698c4ae` Merge PR #245 — chore/support-contact-email
- `06521fc` feat(support): surface SUPPORT_EMAIL across contact touchpoints

## Actions Taken

Nenhuma ação de remediação — execução read-only. O site e a API responderam HTTP 200 à primeira tentativa, pelo que não foram necessárias re-tentativas. O keeper NÃO foi tocado (monitorização read-only conforme instrução).

## Requires Attention

1. **Reforçar gas do keeper (🟡):** enviar ETH para `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` na Base. Saldo atual 0.00706 ETH — acima do crítico mas abaixo do aviso. Recomenda-se top-up antes de se aproximar de 0.002 ETH.
2. **Atualizar `.env.local` (🟡):** sincronizar `MONITOR_SECRET` e `SUPABASE_SERVICE_ROLE_KEY` com os valores atuais no Vercel. Ambos estão desatualizados e a cegar as verificações de Monitor e de saúde da execução DCA. Após atualizar, re-executar a verificação DCA para confirmar que o keeper está a executar os chunks dentro do intervalo esperado.

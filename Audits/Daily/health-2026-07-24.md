# TeraSwap Daily Health Report — 2026-07-24

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.50s |
| API Health | ✅ | HTTP 200, 0.72s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~49 dias) |
| Monitor | ⚠️ | HTTP 401 — `MONITOR_SECRET` local desatualizado (rotacionado na Vercel, não no `.env.local`) |
| Keeper gas (0x71f5) | ⚠️ | 0.00736 ETH em Base (abaixo do threshold de aviso 0.01 ETH; acima do crítico 0.002 ETH) |
| DCA execution | ⚠️ | Não verificável — `SUPABASE_SERVICE_ROLE_KEY` local inválida ("Invalid API key"); gap de check local, não falha do keeper |
| Git Activity (origin/main) | ℹ️ | 31 commits nas últimas 24h (merges de PRs #294, #307, #336–#346 — inclui fix/keeper-eth-usd-feed-chainaware, sprint/keeper-multichain-arbitrum, deps batch 5/6, DCA no-feed consent, nav fix) |
| KV / Rate Limiter | ✅ | Inferido saudável via API Health (HTTP 200) |

## Actions Taken
Nenhuma — nenhum check retornou não-200 nos endpoints públicos, pelo que não foi necessário retry. Nenhuma ação de escrita foi executada (fora do âmbito deste check, read-only).

## Requires Attention
- **Segredos locais desatualizados:** atualizar `MONITOR_SECRET` e `SUPABASE_SERVICE_ROLE_KEY` em `.env.local` para refletir os valores atuais da Vercel/Supabase — sem isto, os checks #4 e #6 continuam cegos em execuções futuras.
- **Gas do keeper baixo:** 0.00736 ETH em Base está abaixo do threshold de warning (0.01 ETH). Não é crítico ainda, mas recomenda-se topar a wallet 0x71f5AC19…0Aa2E5 antes de esgotar (crítico a partir de 0.002 ETH, ponto em que o keeper deixaria de conseguir pagar gas e o DCA pararia).
- Sem visibilidade sobre execuções DCA / ordens ativas devido à chave Supabase inválida — recomenda-se validar manualmente no dashboard Supabase até a chave local ser corrigida.

## Erro na persistência (PERSIST step)
`node scripts/commit-audit-report.mjs` falhou: `fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured` (no worktree temporário `audit-cadence-*`). Este ambiente sandbox não tem a chave SSH de assinatura configurada, pelo que o script não conseguiu criar o commit assinado. Detetou 16 relatórios pendentes de commit (Daily 07-09 a 07-24, Weekly 07-13 e 07-20) — nenhum foi commitado. Não houve retry, stash, nem force, conforme instrução. Requer commit manual do owner ou execução num ambiente com a chave de assinatura disponível.

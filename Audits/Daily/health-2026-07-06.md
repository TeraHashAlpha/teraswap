# TeraSwap Daily Health Report — 2026-07-06

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.91s |
| API Health | ✅ | HTTP 200, 0.42s |
| SSL Certificate | ✅ | Expira 11 Set 2026 (67 dias) |
| Monitor | ⚠️ | HTTP 401 — segredo local `MONITOR_SECRET` desatualizado (rodado no Vercel, não localmente). Não é falha de produção. |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH em Base (acima do limiar 0.01, mas a descer — vigiar) |
| DCA execution | ⚠️ | `Invalid API key` — `SUPABASE_SERVICE_ROLE_KEY` local desatualizada. Verificação cega localmente; não é falha do keeper (`NEXT_PUBLIC_SUPABASE_URL` presente e correto). |
| Git Activity (origin/main) | ℹ️ | 7 commits nas últimas 24h (fetch de origin/main OK) |
| KV / Rate Limiter | ✅ | HTTP 200 (inferido de /api/health) |

## Actions Taken
Nenhuma ação de escrita executada (execução read-only, conforme mandato). Sem retries necessários — site e API responderam HTTP 200 à primeira tentativa. Keeper NÃO tocado (monitorização read-only apenas).

## Requires Attention

Dois segredos locais desatualizados continuam a cegar verificações no ambiente de execução (não afetam produção — persistem desde os relatórios anteriores):

1. **`MONITOR_SECRET`** (`.env.local`) — rodado no Vercel mas não atualizado localmente → check do Monitor devolve 401. Atualizar a variável local a partir do Vercel para restaurar a visibilidade do endpoint `/api/monitor`.
2. **`SUPABASE_SERVICE_ROLE_KEY`** (`.env.local`) — desatualizada → `Invalid API key` no Supabase, impedindo a verificação da saúde de execução DCA. Atualizar a partir do painel Supabase para voltar a monitorizar execuções DCA e ordens ativas.

**Nota operacional (keeper gas):** o saldo de gás do keeper desceu de **0.01199 ETH (05-Jul)** para **0.01149 ETH (06-Jul)** em Base — cerca de −0.0005 ETH/dia. Está agora apenas ~0.0015 ETH acima do limiar de WARNING (0.01 ETH). Ainda não é crítico, mas ao ritmo atual cruzará o limiar em ~2-3 dias. Recomenda-se **top-up preventivo** da wallet executora `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` para evitar alerta iminente e garantir que o motor de ordens DCA continua a poder pagar gás.

**Atividade de código:** 7 commits mergeados em `origin/main` nas últimas 24h (0 no dia anterior) — destaque para o merge do **PR #263** (`chore/splitroute-chainid`: thread do `chainId` ativo nas sub-quotes de split routing, ativando split routing em Base), mais o audit follow-up de chain-awareness (#262) e dois merges Dependabot (undici 6.27.0 no order-engine, actions/checkout 7.0.0). Alinhado com o open loop "merge #263" registado em memória.

**Nota de cobertura:** enquanto a chave Supabase local não for atualizada, a saúde de execução DCA não pode ser confirmada por esta verificação diária — o saldo de gás do keeper (positivo) permanece o único sinal indireto de que o motor de ordens continua financiado e operacional.

# TeraSwap Daily Health Report — 2026-05-04

**Overall Status:** 🟡 WARNING

## Resultados

| Check | Status | Detalhes |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200 (via 307 redirect), 0.95s response |
| API Health | ✅ | HTTP 200 (via 307 redirect), 1.52s response — `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira 2026-07-14 (71 dias restantes) |
| Monitor | ⚠️ | `{"error":"Unauthorized"}` — autenticação rejeitada com MONITOR_SECRET e MONITOR_CRON_SECRET |
| Git Activity | ℹ️ | 1 commit nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido do API Health OK — sem indicação de problemas no KV |

## Detalhes dos Checks

### 1. Site Availability
- Request inicial retorna HTTP 307 (redirect), seguido de HTTP 200.
- Tempo de resposta: **0.95s** — dentro do limiar normal (< 3s).

### 2. API Health
- Endpoint `/api/health` retorna HTTP 200 após redirect.
- Tempo de resposta: **1.52s** — normal.
- Body: `{"status":"OK","timestamp":"2026-05-04T07:06:38.515Z","hint":"Provide ?token=HEALTH_TOKEN for detailed diagnostics."}`
- Nota: diagnósticos detalhados requerem HEALTH_TOKEN (não testado).

### 3. SSL Certificate
- Expira: **2026-07-14** (71 dias restantes).
- Bem acima dos limiares de WARNING (14d) e CRITICAL (7d).

### 4. Monitor Endpoint
- **⚠️ WARNING:** Endpoint `/api/monitor` retorna `{"error":"Unauthorized"}` tanto com `MONITOR_SECRET` como com `MONITOR_CRON_SECRET` do `.env` local.
- Possíveis causas: secrets no `.env` local desatualizados face ao deploy atual, ou o endpoint espera um header/formato diferente.
- Isto não indica necessariamente um problema em produção — o monitor via Cloudflare Worker pode estar funcional independentemente.

### 5. Git Activity (últimas 24h)
- **1 commit:**
  - `4104f53` — chore(ci): remove redundant deploy-preview workflow

### 6. KV / Rate Limiter
- O endpoint de health não expõe status do KV diretamente.
- Inferido como operacional dado que o API Health retorna OK (rate limiter depende do Upstash Redis).

## Actions Taken
- Nenhuma ação necessária. Checks executados em modo read-only.

## Requires Attention

- **Monitor Auth (⚠️):** Verificar se os secrets `MONITOR_SECRET` / `MONITOR_CRON_SECRET` no `.env` local estão sincronizados com os valores no Vercel. Este warning é recorrente — considerar atualizar o `.env` local ou documentar o método correto de autenticação para o health check automatizado.

# TeraSwap Daily Health Report — 2026-06-07

**Overall Status:** 🟢 NORMAL

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 307 → 200, 0.40s (apex redireciona para www.teraswap.app, normal) |
| API Health | ✅ | HTTP 307 → 200, 0.31s — `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira Jul 14 2026 07:15 GMT (~37 dias restantes) |
| Monitor | ✅ | Sem anomalias, sem eventos de segurança, 0 alertas |
| Git Activity | ℹ️ | 0 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido de API saudável (health endpoint OK) |

## Detalhe das verificações

### 1. Disponibilidade do site
O domínio apex `teraswap.app` responde com **HTTP 307** e redireciona para `https://www.teraswap.app/`, que devolve **HTTP 200** em ~0.7s (apex 0.40s + www). Comportamento esperado — redirect apex→www. Sem degradação de performance (bem abaixo do limiar de 3s).

### 2. API Health
`/api/health` segue o mesmo redirect apex→www e devolve **HTTP 200** com payload `{"status":"OK","timestamp":"2026-06-07T07:06:58Z"}`.

### 3. Certificado SSL
Válido até **14 Jul 2026 07:15:32 GMT**. Restam ~37 dias — acima do limiar de WARNING (14 dias) e CRITICAL (7 dias). Sem ação necessária; renovação automática (Vercel) deverá ocorrer antes do fim do prazo.

### 4. Monitor endpoint
Sem anomalias: `volumeZScore=0`, `tradeFreqZScore=0`, `failRate24h=0`, `alerts=[]`. Secção `security` vazia (`recentEvents=[]`). Métricas de atividade:

- **All-time:** 37 trades, $301.61 volume, $0.30 fees, 8 wallets, 4 failed.
- **Últimas 24h:** 0 trades (esperado — executor parado até L2).
- **Últimos 7d:** 10 trades, $293.65 volume, 0 failed.
- **Source health:** kyberswap líder (win rate 68.9%, 0% failures), velora 29.0%, uniswapv3 4.3%, cowswap 0.7%, openocean 100% (n=2). Nenhuma source com failure rate elevada.

### 5. Atividade Git (24h)
**0 commits** nas últimas 24 horas. Sem alterações ao código no período.

### 6. KV / Rate Limiter
O health endpoint não expõe estado KV explícito; saúde inferida da disponibilidade da API (HTTP 200). Sem indicadores de falha.

## Actions Taken
Nenhuma ação corretiva necessária — todos os endpoints devolveram 200 à primeira tentativa, não foram precisos retries. Bot em modo read-only (sem reinícios, alterações de código ou commits), conforme especificação.

## Requires Attention
Tudo OK — nenhuma intervenção manual necessária. Duas notas operacionais (não-bloqueantes) para o autor do script de health-check:

1. **Redirect apex→www devolve 307 sem `-L`.** Os checks 1, 2 e 6 do script (curl sem `--location`) reportam HTTP 307 em vez de 200, podendo gerar falsos negativos. Recomenda-se adicionar `-L` aos curls, ou apontar diretamente para `https://www.teraswap.app`.
2. **Check do Monitor falha com 401 via apex.** Como o redirect 307 muda de host (`teraswap.app` → `www.teraswap.app`), o curl descarta o header `Authorization` por segurança, resultando em 401 Unauthorized. Ao apontar diretamente para `https://www.teraswap.app/api/monitor` a autenticação funciona e os dados são devolvidos corretamente. Recomenda-se atualizar o endpoint do check 4 para o host `www`.

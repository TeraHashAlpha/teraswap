# TeraSwap Daily Health Report — 2026-07-20

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.57s |
| API Health | ✅ | HTTP 200, 0.37s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~53 dias) |
| Monitor | ⚠️ | 401 Unauthorized — MONITOR_SECRET local stale (rodado no Vercel, não actualizado em `.env.local`). Gap de check LOCAL, não outage de produção. |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH na Base — acima do threshold WARNING (0.01), mas com margem mínima (~15%). Top-up recomendado em breve. |
| DCA execution | ⚠️ | "Invalid API key" no Supabase — SUPABASE_SERVICE_ROLE_KEY local stale. Gap de check LOCAL: saúde DCA **não verificável** hoje (não é evidência de falha do keeper). |
| Git Activity (origin/main) | ℹ️ | 1 commit nas últimas 24h: `18695c5` Merge PR #311 (fix/swap-approve-stale-success) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health HTTP 200 |

## Actions Taken
None required — todos os endpoints responderam à primeira tentativa; nenhum retry necessário. Execução read-only (keeper não tocado).

## Requires Attention
1. **⚠️ Actualizar `SUPABASE_SERVICE_ROLE_KEY` em `.env.local`** — a chave local está stale, o que cega o check diário de execução DCA. Enquanto não for actualizada, este relatório não consegue confirmar se o keeper está a executar chunks (DCA está LIVE na Base — este é o check mais importante que está cego).
2. **⚠️ Actualizar `MONITOR_SECRET` em `.env.local`** — 401 no endpoint `/api/monitor`; secret rodado no Vercel mas não localmente.
3. **ℹ️ Gas do keeper (0x71f5) a 0.01149 ETH** — ainda ✅, mas apenas ~15% acima do threshold de WARNING. Planear top-up antes de cair abaixo de 0.01 ETH.

## Notas
- Ambos os itens ⚠️ são gaps de secrets LOCAIS (classificados WARNING por cegarem checks), não incidentes de produção. Site, API, SSL e gas do keeper verificados directamente — todos saudáveis.
- `git fetch origin` OK (com warnings inócuos de unlink em objects temporários — filesystem sandbox, sem impacto).

## Persist — ERRO (não retried, conforme instruções)
`node scripts/commit-audit-report.mjs` falhou (exit 1):

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
```

O ambiente sandbox não tem a signing key SSH configurada, e a regra #12 (commits assinados) impede o commit. **Backlog acumulado: 12 relatórios por persistir** (Daily 07-09→07-18 + 07-20, Weekly 07-13) — o persist está a falhar há vários dias pelo mesmo motivo. Acção manual: correr o script numa sessão com a signing key configurada, ou configurar `user.signingkey` para o ambiente do scheduled task.

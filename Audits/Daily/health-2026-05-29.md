# TeraSwap Daily Health Report — 2026-05-29

**Overall Status:** 🟡 WARNING

## Resultados

| Check | Status | Detalhes |
|-------|--------|----------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.85s resposta (307→200 redirect) |
| API Health | ✅ | HTTP 200, 0.79s resposta. `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira 14 Jul 2026 (46 dias restantes) |
| Monitor | ⚠️ | Endpoint retornou `{"error":"Unauthorized"}` — MONITOR_SECRET presente em .env mas rejeitado pelo servidor |
| Git Activity | ℹ️ | 27 commits nas últimas 24h (Sprints 34–40, incluindo PRs #101–#104) |
| KV / Rate Limiter | ✅ | Inferido da disponibilidade do API Health (status OK) |

## Acções Tomadas

- Retry não foi necessário — site e API responderam com HTTP 200 na primeira tentativa.
- Monitor testado com `MONITOR_SECRET` do `.env` local — resposta `Unauthorized`. Possível rotação de secret pós-Vercel breach (ref: INC Vercel 2026-04-21) ou dessincronização entre `.env` local e variável Vercel.

## Requer Atenção

- **🟡 Monitor Endpoint Unauthorized:** O secret local `MONITOR_SECRET` não é aceite pelo endpoint `/api/monitor`. Verificar se:
  1. O secret foi rotacionado no Vercel dashboard sem actualizar o `.env` local.
  2. O header esperado é diferente (e.g., `MONITOR_CRON_SECRET` vs `MONITOR_SECRET`).
  3. O endpoint espera um token diferente (e.g., `HEALTH_TOKEN` conforme hint do `/api/health`).

---

*Relatório gerado automaticamente pelo Daily Health Bot — 2026-05-29T07:06Z*

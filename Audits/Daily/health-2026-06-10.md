# TeraSwap Daily Health Report — 2026-06-10

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.53s (redirect 308 → https://www.teraswap.app/, normal) |
| API Health | ✅ | HTTP 200, 0.91s — `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira a 14 Jul 2026 07:15 GMT (34 dias restantes) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — secret local não corresponde ao de produção |
| Git Activity | ℹ️ | 1 commit nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido OK a partir da disponibilidade da API (endpoint /api/health sem bloco KV detalhado sem token) |

## Detalhe das verificações

**1–2. Disponibilidade e tempo de resposta.** Tanto a raiz como o endpoint `/api/health` respondem HTTP 200 após o redirect permanente 308 para `www.teraswap.app` (comportamento normal de canonicalização). Tempos de resposta de 0,53s e 0,91s, bem abaixo do limiar de WARNING (3s).

**3. Certificado SSL.** Válido até 14 Jul 2026, 34 dias restantes — acima do limiar de WARNING (14 dias). Sem ação necessária; renovação automática esperada via Vercel.

**4. Monitor endpoint.** Retornou `HTTP 401 {"error":"Unauthorized"}`. O `MONITOR_SECRET` está presente no `.env` local e foi enviado no header `Authorization: Bearer`, mas foi rejeitado. Causa provável: rotação do secret em produção (Vercel env) sem atualização do `.env` local, ou `.env` local desatualizado. Classificado como **WARNING** conforme a spec da task — não crítico, pode também indicar manutenção do monitor. **Não houve exposição do secret** (verificação feita em subshell, valor redigido nos logs).

**5. Atividade Git (24h).** 1 commit:
- `a7f0692` — docs(incident): add INC-2026-06-09-001 (qr@0.6.0 connect-modal crash)

Apenas documentação de incidente; sem alterações a contratos ou fluxos de fundos.

**6. KV / Rate Limiter.** O endpoint `/api/health` sem token devolve apenas estado resumido (`status: OK`), sem bloco KV detalhado. Saúde do KV inferida como OK a partir da disponibilidade da API. Para diagnóstico detalhado seria necessário `?token=HEALTH_TOKEN`.

## Actions Taken
Nenhum retry necessário — site e API responderam 200 à primeira tentativa. Sem reinício de processos, alterações de código ou commits (bot read-only). Tentativa adicional de validação do monitor com secret sem espaços em branco — manteve 401.

## Requires Attention
- **Monitor endpoint (401):** verificar se o `MONITOR_SECRET` foi rotacionado em produção (Vercel) e sincronizar o `.env` local, ou confirmar se o monitor está em manutenção. Enquanto não resolvido, a verificação de anomalias do monitor (taxas de erro, eventos de segurança) fica indisponível neste relatório diário.

---
*Relatório gerado automaticamente pela task agendada `teraswap-daily-health`. Executor process intencionalmente parado (L2 pendente) — não verificado, conforme instruções.*

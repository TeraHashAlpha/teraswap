# TeraSwap Daily Health Report — 2026-06-06

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.71s — apex faz 307 → `https://www.teraswap.app/` (redirect canónico esperado) |
| API Health | ✅ | HTTP 200, 0.84s — body `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira Jul 14 2026 07:15 GMT (38 dias restantes) |
| Monitor | ⚠️ | HTTP 401 `Unauthorized` — `MONITOR_SECRET` existe no `.env` mas foi rejeitado |
| Git Activity | ℹ️ | 16 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido de disponibilidade da API (health básico não expõe estado KV) |

## Detalhe

**Site & API:** ambos respondem 200 após o redirect 307 do apex para `www` — comportamento normal de canonicalização, não é falha. Tempos de resposta < 1s, bem abaixo do limiar de 3s.

**SSL:** 38 dias de margem, acima do limiar de WARNING (14 dias). Sem ação necessária.

**Monitor (⚠️):** o endpoint `/api/monitor` devolveu `401 Unauthorized`. A chave `MONITOR_SECRET` está definida no `.env` local, portanto a causa mais provável é um **desfasamento de segredo** — o valor local está desatualizado face ao que está em produção (Vercel). Isto é consistente com a existência do runbook de rotação de segredos (`vercel-breach rotation`) no histórico recente. Classificado como WARNING conforme a regra do task (não-crítico; pode ser manutenção/rotação).

**Git:** 16 commits nas últimas 24h, foco no Sprint 9X (bounding da rota de quote com `maxDuration`, guard de fetch no cliente para HTML/non-JSON, FEEDBACK do 9X) e merge do 9W (wrapped-native chain-aware no adapter CoW). Último commit `bce13cb` (2026-06-05 19:55).

## Actions Taken

Nenhuma ação correctiva. Site e API devolveram 200 (após redirect canónico esperado), pelo que não foram necessárias as 3 retentativas. Bot em modo read-only — sem restart de processos, sem alterações a código, sem commits. Executor permanece intencionalmente parado (até L2), não verificado.

## Requires Attention

- **Monitor 401 (⚠️):** validar/rotacionar `MONITOR_SECRET` — confirmar que o valor no `.env` local corresponde ao segredo activo no Vercel. Se houve rotação recente (ver runbook vercel-breach), actualizar o `.env` local. Até resolução, este check fica cego.
- Restantes verificações: tudo OK, sem intervenção manual.

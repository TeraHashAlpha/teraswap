# TeraSwap Daily Health Report — 2026-07-16

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.68s |
| API Health | ✅ | HTTP 200, 0.38s |
| SSL Certificate | ✅ | Expira 2026-09-11 (~57 dias) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — `MONITOR_SECRET` local desatualizado (stale-local-secret); NÃO é outage de produção |
| Keeper gas (0x71f5) | ✅ | 0.01149 ETH na Base — acima do limiar WARNING (0.01) mas marginal; top-up recomendado em breve |
| DCA execution | ⚠️ | Supabase devolveu "Invalid API key" — `SUPABASE_SERVICE_ROLE_KEY` local desatualizada (stale-local-key); saúde DCA NÃO verificável hoje, não é falha do keeper |
| Git Activity (origin/main) | ℹ️ | 0 commits nas últimas 24h (fetch OK, verificado contra origin/main) |
| KV / Rate Limiter | ✅ | Inferido saudável via API health (HTTP 200) |

## Actions Taken

None required — todos os endpoints de produção responderam à primeira tentativa; sem retries.

## Requires Attention

1. 🟡 **Atualizar `MONITOR_SECRET` em `.env.local`** — o secret foi rodado no Vercel mas não localmente; o check do monitor está cego até ser atualizado.
2. 🟡 **Atualizar `SUPABASE_SERVICE_ROLE_KEY` em `.env.local`** — chave local inválida; os checks de execução DCA e ordens ativas ficam cegos até correção. Enquanto ambos os secrets estiverem stale, este relatório não consegue confirmar a saúde do order engine DCA (apenas gas do keeper + disponibilidade do site).
3. ℹ️ **Gas do keeper marginal** — 0.01149 ETH, apenas ~15% acima do limiar WARNING; considerar top-up do executor 0x71f5 na Base antes de descer abaixo de 0.01.
4. 🟡 **Backlog de relatórios não persistidos** — o script de persist encontrou 9 relatórios por commitar (Daily 07-09 a 07-16 + Weekly 07-13) mas falhou (ver abaixo); os relatórios existem apenas no disco local até correção.

## Persist Error (scripts/commit-audit-report.mjs)

O passo de persist falhou nesta sessão — commit rejeitado por falta de chave de assinatura no ambiente:

```
fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured
(+ warnings "unable to unlink .git/objects/... Operation not permitted")
```

Não foi feito retry (conforme instruções). Ação manual: correr `node scripts/commit-audit-report.mjs` numa sessão com a chave SSH de assinatura configurada (cf. `docs/Runbooks/SIGNED-COMMITS.md`) para persistir os 9 relatórios pendentes em `audits/cadence`.

# TeraSwap Daily Health Report — 2026-07-04

**Overall Status:** 🟡 WARNING

Dois secrets locais desatualizados estão a cegar duas verificações (Monitor e chave Supabase). Não há indício de falha de produção nem do keeper: site, API, SSL, gas do keeper e KV estão todos saudáveis.

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.805s |
| API Health | ✅ | HTTP 200, 0.497s |
| SSL Certificate | ✅ | Expira 11 Set 2026 (69 dias) |
| Monitor | ⚠️ | HTTP 401 — segredo local desatualizado (MONITOR_SECRET rodado no Vercel, não atualizado no `.env.local`) |
| Keeper gas (0x71f5) | ✅ | 0.01249 ETH na Base (acima do limiar 0.01, mas próximo — monitorizar) |
| DCA execution | ⚠️ | "Invalid API key" — SUPABASE_SERVICE_ROLE_KEY local desatualizada; execução DCA não verificável por esta via (gap de verificação LOCAL, NÃO falha do keeper) |
| Git Activity (origin/main) | ℹ️ | 11 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | HTTP 200 (inferido da API) |

### Nota sobre a atividade Git (origin/main, últimas 24h)

11 commits — foco em correção de fontes de cotação e alertas de monitorização:

- `b600a05` Merge PR #261 — sushi v7 redsnwapper quote fix
- `d6adcab` Merge branch 'main' into chore/sushi-v7-redsnwapper-quote-fix
- `f1c9000` Merge PR #260 — quote-quorum-hardening
- `85bcced` fix(sushi): v7 requires sender — restore quotes both chains; scope Sushi (+W7-L-02 trio) quote-only
- `0e3ea7b` feat(monitor): source-health alerting — silence/outlier/drift pages once per window [W7-L-02]
- `5bea71f` fix(quotes): low-quorum display sanity — a mis-scaled quote cannot win a 2-responder display [W7-L-02]
- `7a71846` Merge PR #259 — quote-source-fixes
- `f85ad05` Merge PR #258 — silent-sources-investigation
- `417682a` audit(w7-followup): silent-sources investigation — 5 root causes, Augustus whitelist gap REFUTED (0%)
- `ffaeb0b` chore(balancer): disable dead source (SOR order endpoint 404) + mark adapter superseded
- `a46cf66` fix(openocean): send HUMAN-unit sell amount to the v4 API (was raw base units, 10^decimals off)

## Actions Taken

Nenhuma ação de escrita foi necessária. Site e API responderam HTTP 200 à primeira tentativa — não foram necessários retries. Nenhum processo foi reiniciado ou modificado (keeper monitorizado apenas em modo read-only, conforme instrução). Apenas este relatório foi escrito.

## Requires Attention

1. **🟡 Segredo Monitor local desatualizado** — `/api/monitor` devolve HTTP 401 com o `MONITOR_SECRET` do `.env.local`. O segredo foi provavelmente rodado no Vercel e não atualizado localmente. É um gap de verificação LOCAL, não uma falha de produção. **Ação:** copiar o `MONITOR_SECRET` atual do Vercel para o `.env.local` para restaurar esta verificação.

2. **🟡 Chave Supabase local desatualizada** — as queries a `order_executions` e `orders` devolvem "Invalid API key". A `SUPABASE_SERVICE_ROLE_KEY` no `.env.local` está desatualizada. Enquanto não for atualizada, a saúde de execução DCA **não é verificável por esta via**. Não é (por si só) uma falha do keeper — o gas do keeper (0.01249 ETH) está saudável, o que sugere que a wallet continua funcional. **Ação:** copiar a `SUPABASE_SERVICE_ROLE_KEY` atual do Supabase para o `.env.local` e re-executar a verificação DCA para confirmar execuções recentes.

3. **🟢→🟡 Gas do keeper a vigiar** — 0.01249 ETH na Base está acima do limiar de WARNING (0.01 ETH), mas com pouca folga. Se cair abaixo de 0.01 ETH, reabastecer a wallet executora `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` na Base. Sem ação imediata necessária.

---

*Relatório gerado automaticamente pela tarefa agendada `teraswap-daily-health`. Verificações executadas em modo read-only. Keeper NÃO foi tocado.*

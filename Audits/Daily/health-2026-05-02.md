# TeraSwap Daily Health Report — 2026-05-02

**Overall Status:** 🟡 WARNING

## Resultados

| Check | Status | Detalhes |
|-------|--------|----------|
| Site (teraswap.app) | ✅ | HTTP 200, 0.637s resposta (redirect 307→200) |
| API Health | ✅ | HTTP 200, 1.499s resposta — `{"status":"OK"}` |
| SSL Certificate | ✅ | Expira 2026-07-14 (73 dias restantes) |
| Monitor | ⚠️ | HTTP 401 Unauthorized — MONITOR_SECRET rejeitado |
| Git Activity | ℹ️ | 0 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | Inferido da disponibilidade do API Health (status OK) |

## Análise Detalhada

### Site & API
Ambos os endpoints respondem correctamente. O site faz redirect 307 antes de servir HTTP 200, comportamento normal para Vercel. Tempos de resposta dentro do aceitável (<3s).

### SSL
Certificado válido até 14 de Julho de 2026 — margem confortável de 73 dias. Sem acção necessária.

### Monitor Endpoint
O endpoint `/api/monitor` retorna HTTP 401 com ambas as variáveis `MONITOR_SECRET` e `MONITOR_CRON_SECRET` do ficheiro `.env` local. Possíveis causas:
1. Secret foi rotacionado no Vercel após o último sync do `.env` local
2. O endpoint espera um header ou parâmetro diferente
3. Alteração na lógica de autenticação do monitor

**Nota:** O `.env` local pode estar desatualizado em relação às variáveis deployadas no Vercel. Isto não indica necessariamente um problema em produção — o cron do Cloudflare Worker pode estar a funcionar correctamente com o secret correcto.

### Git Activity
Sem commits nas últimas 24 horas. Sprint 9B aguarda deploy do P68 — consistente com o estado actual do projecto.

### KV / Rate Limiter
Saúde inferida a partir da resposta bem-sucedida do endpoint `/api/health`. Sem indicação de problemas no rate limiter.

## Actions Taken
- Nenhuma. Relatório apenas de leitura.

## Requires Attention
- **⚠️ Monitor Auth:** Verificar se o `MONITOR_SECRET` local está sincronizado com o Vercel. Executar `vercel env pull` ou comparar manualmente. Se o cron do Cloudflare Worker continua a funcionar (verificar Telegram para alertas), o problema é apenas local e de baixa prioridade.

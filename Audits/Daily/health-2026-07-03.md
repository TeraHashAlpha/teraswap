# TeraSwap Daily Health Report — 2026-07-03

**Overall Status:** 🟡 WARNING

> Sem outage de produção. O estado WARNING deve-se a **dois segredos locais desatualizados** (`MONITOR_SECRET` e `SUPABASE_SERVICE_ROLE_KEY` em `.env.local`) que cegam duas verificações. Todos os endpoints públicos de produção respondem normalmente.

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.43s |
| API Health | ✅ | HTTP 200, 0.57s |
| SSL Certificate | ✅ | Expira Sep 11 2026 (~70 dias) |
| Monitor | ⚠️ | 401 Unauthorized — `MONITOR_SECRET` local desatualizado (rodado no Vercel, não atualizado em `.env.local`). Lacuna de verificação local, não é outage. |
| Keeper gas (0x71f5) | ✅ | 0.01300 ETH na Base (acima do limiar WARNING de 0.01, mas com margem curta — recarregar em breve) |
| DCA execution | ⚠️ | "Invalid API key" — `SUPABASE_SERVICE_ROLE_KEY` local desatualizado. Lacuna de verificação local; **não** é falha do keeper. Não foi possível confirmar execuções/ordens ativas por esta via. |
| Git Activity (origin/main) | ℹ️ | 15 commits nas últimas 24h (mais recente: Jul-02 23:46) |
| KV / Rate Limiter | ✅ | HTTP 200 (inferido de /api/health disponível) |

## Actions Taken

Nenhuma ação de escrita/reinício executada (modo read-only). O keeper **não** foi tocado.

- Site e API responderam HTTP 200 à primeira tentativa — sem necessidade de retries.
- `git fetch origin` bem-sucedido; contagem de commits validada contra `origin/main` (não sobre o checkout local).

## Requires Attention

1. **⚠️ `MONITOR_SECRET` local desatualizado** — atualizar `.env.local` com o valor atual do Vercel para reativar a verificação do endpoint `/api/monitor`. Sem isto, a deteção de anomalias via monitor fica cega localmente.
2. **⚠️ `SUPABASE_SERVICE_ROLE_KEY` local desatualizado** — atualizar `.env.local` com a chave `service_role` atual do Supabase. Enquanto não for corrigido, a verificação de saúde de execução DCA não consegue ler `order_executions`/`orders`. **Nota de mitigação:** o gas do keeper (0.013 ETH, Check 5) confirma que a carteira executora está financiada e operacional; a lacuna é apenas de visibilidade local, não indício de falha do engine.
3. **ℹ️ Keeper gas com margem curta** — 0.013 ETH está apenas ~0.003 ETH acima do limiar WARNING (0.01). Não é crítico hoje, mas convém agendar recarga da carteira executora `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` na Base para evitar cair em WARNING/CRITICAL nos próximos dias.

---
*Gerado automaticamente pela tarefa agendada `teraswap-daily-health`. Verificações 1–3, 5, 7, 8 confirmadas em produção; 4 e 6 bloqueadas por segredos locais desatualizados.*

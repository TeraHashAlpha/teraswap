# TeraSwap Daily Health Report — 2026-07-02

**Overall Status:** 🟡 WARNING

Produção 100% saudável (site, API, SSL, keeper, KV). O estado WARNING deve-se apenas a **dois segredos locais desatualizados** que cegam as verificações do Monitor e da saúde de execução DCA — não é uma falha de produção nem do keeper.

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.885s |
| API Health | ✅ | HTTP 200, 0.378s |
| SSL Certificate | ✅ | Expira Sep 11 2026 (~71 dias) |
| Monitor | ⚠️ | HTTP 401 — `MONITOR_SECRET` local desatualizado (stale-local-secret) |
| Keeper gas (0x71f5) | ✅ | 0.01350 ETH on Base (acima do limiar 0.01, mas a aproximar-se) |
| DCA execution | ⚠️ | "Invalid API key" — `SUPABASE_SERVICE_ROLE_KEY` local desatualizado (stale-local-key); NÃO é falha do keeper |
| Git Activity (origin/main) | ℹ️ | 15 commits nas últimas 24h |
| KV / Rate Limiter | ✅ | HTTP 200 (inferido a partir de /api/health) |

## Detalhe das verificações

**1–2. Disponibilidade e API** — Site e `/api/health` respondem HTTP 200, ambos bem abaixo do limiar de 3s. Sem retries necessários.

**3. SSL** — Certificado válido até 11 Set 2026, ~71 dias de margem. Muito acima do limiar de aviso (14 dias).

**4. Monitor** — Endpoint devolveu `{"error":"Unauthorized"}` (HTTP 401). Conforme o runbook, isto indica que o `MONITOR_SECRET` no `.env.local` está desatualizado (rodado no Vercel, não atualizado localmente). Classificado como **lacuna de verificação local**, não como falha de produção. Ação manual: atualizar `MONITOR_SECRET` no `.env.local`.

**5. Keeper — saldo de gás (Base)** — Executor `0x71f5AC191587AE132D966a719569b2468e0Aa2E5` com **0.01350 ETH**. Acima do limiar de WARNING (0.01 ETH), logo NORMAL. Nota: está a ~0.0035 ETH da linha de aviso — recomenda-se um top-up preventivo em breve para evitar interrupção da execução DCA autónoma. (Verificação read-only; keeper NÃO tocado.)

**6. Saúde de execução DCA (Supabase)** — Ambas as queries (`order_executions` e `orders` ativos) devolveram `{"message":"Invalid API key"}`. Conforme o runbook, isto significa que o `SUPABASE_SERVICE_ROLE_KEY` local está desatualizado — **lacuna de verificação local**, NÃO uma falha do keeper. Não foi possível confirmar a última execução nem as ordens DCA ativas por este canal. O saldo de gás saudável do keeper (check 5) e a atividade recente do repositório sugerem operação normal, mas a confirmação via Supabase fica pendente até atualização da chave. Ação manual: atualizar `SUPABASE_SERVICE_ROLE_KEY` no `.env.local`.

**7. Atividade Git (origin/main)** — `git fetch` bem-sucedido. **15 commits** nas últimas 24h (2026-07-01 18:15 → 2026-07-02 00:03). HEAD: `cb0748d` (Merge PR #250, chore/ohm-knc-remap). Temas principais: pipeline de catálogo de tokens cross-verified (#248–#250), DCA execution-quality defer gate / cross-agg deviation guard, e advisory para tokens oracle-less. Atividade elevada mas coerente com o trabalho de Fase 2 (expansão de fontes / catálogo).

**8. KV / Rate Limiter** — Inferido saudável a partir de `/api/health` (HTTP 200).

## Actions Taken

- Nenhuma ação de escrita ou reinício. Todas as verificações foram read-only.
- Site e API responderam 200 à primeira — sem retries necessários.
- Keeper monitorizado apenas em leitura (saldo de gás on-chain). Não reiniciado nem modificado.

## Requires Attention

1. **🟡 `MONITOR_SECRET` local desatualizado** — atualizar em `.env.local` (foi rodado no Vercel). Cega a verificação do Monitor até ser corrigido.
2. **🟡 `SUPABASE_SERVICE_ROLE_KEY` local desatualizado** — atualizar em `.env.local`. Impede a confirmação da saúde de execução DCA por este canal. Produção/keeper provavelmente OK (gás saudável), mas sem confirmação direta.
3. **ℹ️ Keeper gas a aproximar-se do limiar** — 0.01350 ETH (limiar WARNING = 0.01). Considerar top-up preventivo do executor `0x71f5` na Base para garantir continuidade da execução DCA 24/7.

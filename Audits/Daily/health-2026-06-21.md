# TeraSwap Daily Health Report — 2026-06-21

**Overall Status:** 🟡 WARNING

> Execução automática do bot de auditoria (read-only). Hora UTC: 2026-06-21T07:07Z.

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (teraswap.app) | ✅ | HTTP 308 → 200 (redirect apex → www.teraswap.app), 0.48–0.85s. Site operacional. |
| API Health | ✅ | HTTP 308 → 200, 0.69s. Corpo: `{"status":"OK"}`. |
| SSL Certificate | ✅ | Expira Sep 11 2026 06:30 GMT (81 dias restantes). |
| Monitor | ⚠️ | `/api/monitor` devolveu `{"error":"Unauthorized"}` apesar de `MONITOR_SECRET` estar presente no `.env` (64 chars). |
| Git Activity | ℹ️ | 0 commits nas últimas 24h. |
| KV / Rate Limiter | ✅ | Inferido saudável a partir da disponibilidade da API (health = HTTP 200). Diagnóstico detalhado de KV requer `?token=HEALTH_TOKEN`, não consultado. |

## Detalhe das verificações

**1–2. Disponibilidade e tempo de resposta.** O domínio apex `teraswap.app` responde com HTTP 308 (redirect permanente) para `https://www.teraswap.app/`. Seguindo o redirect, tanto o site como `/api/health` resolvem para HTTP 200 com tempos de resposta bem abaixo do limiar de 3s (0.48s–0.85s). Não é uma falha — é a configuração normal de redirect apex→www. O site está operacional.

**3. Certificado SSL.** Válido até 11 de setembro de 2026. Restam 81 dias, muito acima do limiar de WARNING (14 dias). Sem ação necessária.

**4. Monitor endpoint.** O endpoint `/api/monitor` rejeitou o pedido com `{"error":"Unauthorized"}`. A variável `MONITOR_SECRET` **está** definida no `.env` local (comprimento 64) e foi corretamente carregada para o shell, pelo que o problema não é um segredo em falta localmente. Causa provável: o segredo foi rodado/alterado em produção (Vercel) e o `.env` local está desatualizado, ou o endpoint está em manutenção. Classificado como WARNING conforme protocolo (não crítico). Como o segredo não autenticou, não foi possível ler métricas de erro/eventos de segurança nesta execução.

**5. Atividade Git (24h).** Nenhum commit no repositório nas últimas 24 horas. Coerente com Sprint 9B em pausa (P68 mainnet deploy pendente).

**6. KV / Rate Limiter.** O `/api/health` básico respondeu 200 sem expor estado de KV (requer `?token=HEALTH_TOKEN`). Saúde do KV inferida da disponibilidade da API. Sem indícios de problema.

## Actions Taken

- Seguido o redirect 308→200 para confirmar que site e API estão efetivamente UP. Como o primeiro follow já devolveu HTTP 200, **não foram necessárias as 3 retentativas** do protocolo (o 308 é um redirect permanente intencional, não uma falha transitória).
- Verificada a presença de `MONITOR_SECRET` no `.env` sem expor o valor (apenas comprimento).
- Nenhum processo reiniciado, nenhum ficheiro de código alterado, nenhum commit efetuado (bot read-only). O executor permanece intencionalmente parado (L2 pendente) — não verificado nem reiniciado.

## Requires Attention

- **Monitor endpoint (⚠️):** alinhar o `MONITOR_SECRET` local com o valor em produção (Vercel env vars) — provável rotação de segredo. Verificar manualmente se `/api/monitor` está apenas em manutenção ou se a auth foi efetivamente alterada. Até resolver, as métricas de erro/eventos de segurança do monitor não são consultáveis por este bot.

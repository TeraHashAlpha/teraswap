# TeraSwap Daily Health Report — 2026-07-26

**Overall Status:** 🟡 WARNING

## Results

| Check | Status | Details |
|-------|--------|---------|
| Site (www.teraswap.app) | ✅ | HTTP 200, 0.62s |
| API Health | ✅ | HTTP 200, 0.41s |
| SSL Certificate | ✅ | Expires 11 Set 2026 (~47 dias) |
| Monitor | ⚠️ | HTTP 401 — segredo local `MONITOR_SECRET` desatualizado (stale-local-secret, não é outage de produção) |
| Keeper gas (0x71f5) | ⚠️ | 0.00736 ETH on Base — abaixo do limiar de 0.01 ETH (acima do crítico 0.002 ETH) |
| DCA execution | ⚠️ | "Invalid API key" no Supabase — `SUPABASE_SERVICE_ROLE_KEY` local desatualizada (stale-local-key, não é falha do keeper) |
| Git Activity (origin/main) | ℹ️ | 0 commits nas últimas 24h (fetch bem-sucedido) |
| KV / Rate Limiter | ✅ | Inferido normal via disponibilidade da API (HTTP 200) |

## Actions Taken

Nenhuma ação corretiva foi necessária — todos os checks HTTP passaram à primeira tentativa (sem necessidade de retries). Nenhum processo foi reiniciado, incluindo o keeper (monitorizado apenas em leitura, conforme instrução).

## Requires Attention

1. **Keeper gas baixo (0.00736 ETH na wallet 0x71f5AC19…):** está abaixo do limiar de warning (0.01 ETH). Ainda não é crítico (>0.002 ETH), mas recomenda-se reabastecer a wallet do executor na Base para evitar que o keeper fique sem gas para executar chunks de DCA.
2. **`MONITOR_SECRET` local desatualizado:** o endpoint `/api/monitor` devolveu 401 ao usar o segredo local — provavelmente foi rodado em Vercel mas não atualizado em `.env.local`. Atualizar localmente para restaurar a visibilidade deste check.
3. **`SUPABASE_SERVICE_ROLE_KEY` local desatualizada:** os endpoints REST do Supabase devolveram "Invalid API key", impedindo verificar execuções recentes de DCA e ordens ativas. Atualizar `.env.local` com a chave atual para restaurar este check. Enquanto isto não for corrigido, o estado real de execução de DCA (stalled ou não) permanece **não verificado** — não confirmado como normal.

## Persist

`node scripts/commit-audit-report.mjs` falhou: `fatal: either user.signingkey or gpg.ssh.defaultKeyCommand needs to be configured` (ambiente sandbox sem chave de assinatura configurada). O relatório de 25-07-2026 também ficou por persistir pelo mesmo motivo (script detetou 2 relatórios pendentes: health-2026-07-25.md e health-2026-07-26.md). Não foi feita nova tentativa nem qualquer ação de stash/force, conforme instrução. Requer intervenção manual do owner para commitar ambos os relatórios em `audits/cadence`.


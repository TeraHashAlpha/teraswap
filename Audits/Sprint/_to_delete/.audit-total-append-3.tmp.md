
### AUDIT V3-PREDEPLOY — final gate of the v3 arc (2026-07-09)

Report: `Audits/Sprint/AUDIT-V3-PREDEPLOY-AUDIT.md`. Base `origin/main` `4277e15` (incl. #301). All 8 audited
commits SSH-signed. Independent checks: EIP-712 v3 typestring byte-compare at pr302 (identical), `.sol`
bitmap-positions vs `v3-nonce-bitmap.ts`, keeper-file byte-identity since sign-off, DCA-exemption traced to the
contract.

- **PR #301 `sprint/v3-p3-cancel-and-hardening` (merged `4277e15`, 5 commits) — CONFIRMED 0C/0H/0M/0L.**
  M-01 CLOSED exactly per prescription (on-chain `fetchErc20Decimals` sole decimals source; client mismatch →
  422; decimals-read-fail → 422 fail-closed; `min()` combine; 6 regression tests on the real POST route incl.
  the spoofed-HIGH-decimals exploit). v3 single cancel refuse-guard NARROWED (`isV3 && !orderExecutorV3`), wired
  to per-chain v3 addr/ABI. Mass-cancel: `v3-nonce-bitmap.ts` mirrors `.sol` bitmapPositions; DCA-exemption
  verified against the contract (bitmap consumed only for `orderType != DCA`; `cancelledOrders` unconditional) →
  client routes v3 DCA → individual `cancelOrder()`, v3 non-DCA → batched `invalidateUnorderedNonces`, union
  covers all classes. 4 gitleaks suppressions = public WETH/USDC in test fixtures, `.gitleaks.toml` untouched.

- **PR #302 `v3 runbook + deploy/verify scripts` (`d147c8b`, draft, docs+scripts only) — DEPLOY-AUTHORIZED
  (0C/0H), conditional on 3 MEDIUM runbook/script amendments (M-A/M-B/M-C) applied before broadcasting §2.**
  Deploy script: env-named params (no embedded addr), plaintext-key guard fires, chain-id assert, admin = v2 EOA
  bounded by the contract's own 48h/7d timelock (parity w/ live v2; Key-Hardening admin→Safe = W1-L-02, not a
  blocker). Verifier: 9 on-chain assertions + oracle checkpoint sufficient EXCEPT feeRecipient/WETH (M-A).
  Runbook: oracle-config (48h) before cutover; keeper/frontend gated on post-verify env; cancel live before
  deploy + e2e smoke; Phase-0 retirement gated on all DCA chains. **Findings (all bounded, none C/H):**
  - **M-A** verifier `run()` + §3 don't assert immutable `feeRecipient()`/`WETH()` → wrong deploy uncaught (fee
    misdirection / ETH-output liveness). Add assertions + §3 check.
  - **M-B** §6 post-first-order rollback contradiction (step 1 "unset both env vars" vs step 2 "keep keeper
    ORDER_EXECUTOR_V3_ADDRESS") — literal step 1 strands existing v3 orders (unexecutable+uncancellable). Unset
    only the frontend signing env.
  - **M-C** router-set muddled: §2 Step 3 "same as v2 (~10)" vs §3 verifier example `[V6]` vs config.ts served
    set (Augustus V6 + Uniswap SwapRouter02). Bootstrap+verify EXACTLY the 2 served routers + event-scan.
  - **L-A** §5 set keeper v3 env before frontend signing to avoid a transient skip-flag window.
- **PART C repo state clean:** no v3 address configured anywhere (env-name-only, `.env.example` unset,
  fail-closed); order-floor.js + submission-policy.js byte-identical since sign-off; frontend EIP-712 v3
  typestring byte-matches the `.sol` at pr302; DEPLOYMENTS.md consistent with runbook §0.
  Commits + AUDIT-TOTAL block left for owner's SSH batch.

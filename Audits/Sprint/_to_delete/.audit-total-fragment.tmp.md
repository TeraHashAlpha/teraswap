
### AUDIT V3-P1 — OrderExecutor v3 contract (sprint/v3-p1-executor-contract, 2026-07-09)

**Verdict: APPROVE-TO-MERGE — 0C / 0H / 0M / 2L / 2I.** Merge gate for the ADR-013 §1–§3 contract; deploy stays
the separate gated **V3-P4** (48h timelock + migration + runbook + on-chain per-chain verification — NOT granted
here). Report: `Audits/Sprint/AUDIT-V3-P1-EXECUTOR-AUDIT.md`. Audited SHA **`954c415`** (6 commits `0240d7f..954c415`,
all SSH-signed), base `origin/main` `8927f8f`. 45 Foundry tests (CI forge 113/0 authoritative — `forge`/`slither`
absent in sandbox).

- **5 deviations adjudicated:** (1) `fairOut==0` → signed-min = **ACCEPT-with-note / L-01**: reachable only on
  dust-value output (< 1 raw tokenOut unit); keeper controls none of the truncation drivers (amountIn/decimals/pair
  are signed), and `scaledMin==0` already reverts so the floor is never the v2 1-wei no-op — bounded, no fund path;
  recommend `revert on hasFeed && fairOut==0` per ADR-013 N4 (non-blocking). (2) fair-value on `netAmount` =
  **ACCEPT** (fee once, `:554` pre-swap; floor 949.05e18 on net 999e18). (3) DCA outside bitmap = **ACCEPT**
  (`cancelledOrders` checked `:452` every path; bitmap keyed to owner/msg.sender — no cross-owner grief). (4) EIP-712
  version "3" = **ACCEPT** (version + typehash + verifyingContract + chainId isolate from v2). (5) timelocked floor
  feed + sequencer 1h grace = **ACCEPT-with-note**: floor feed 48h-only (P6 closed), sequencer-down/stale/incomplete
  → NO-FEED → signed min (never blind); instant `setOracleConfig` is trigger-only (v2 parity, I-02).
- **Standing checks all pass:** 1-wei clamp gone (revert-only), `maxSlippageBps≤500`, `max(oracle,scaledMin)` floor,
  decimals-safe `mulDiv`, `recipient==owner`, balance-delta parity, router default-deny (chain-correct V5/V6 = V3-P4
  deploy duty — no hardcoded router), `nonReentrant`+CEI (reentrant nonce-1 not consumed), Permit2 bitmap (no
  cross-word replay; `invalidateUnorderedNonces` only `msg.sender`), non-DCA ZeroHash rejected (P1c), out-of-order
  exec safe (P1b), no new admin surface beyond the timelocked floor config.
- **Coverage gaps (non-blocking):** L-02 — decimals fuzz + sequencer tests hit only the `_fairValueOut` helper, not
  `executeOrder`; the `fairOut==0` branch is untested. I-01 — `MIN_ORDER_AMOUNT` is raw-unit (USD-min at signing +
  `/api/orders` dust rejection = V3-P2). Remediation prompt for L-01 in the report. Commit left for owner's SSH batch.

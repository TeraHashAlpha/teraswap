
### AUDIT V3-P2 + followups — joint gate, TWO verdicts (2026-07-09)

Report: `Audits/Sprint/AUDIT-V3-P2-AND-FOLLOWUPS-AUDIT.md`. Base `origin/main` `a81cb4c` (V3 base #296 `954c415`
merged). All 8 commits SSH-signed. Independent checks: EIP-712 v3 typestring byte-compare (identical), keeper
routing `node --test` 7/7, on-chain-decimals trust trace. CI authoritative: forge 119/0, vitest 2653/2653,
keeper 180/180.

- **PR #298 `chore/v3-audit-followups` (`7b39478`, 3 commits) — APPROVE-TO-MERGE, 0C/0H/0M/0L.** Delta pass only.
  The L-01 fix is exactly `if (fairOut==0) revert OracleValueZero()` INSIDE `if (hasFeed)` — genuine
  no-feed/stale/sequencer-down return hasFeed=false and skip it (approved signed-min semantics unchanged);
  stricter never looser. Diff hygiene clean (6 contract lines + tests + spec; NO forge-std/OZ submodules;
  constants/nonces/routerDataHash/events/MAX_ORDER_SLIPPAGE_BPS untouched). L-02 now genuinely end-to-end: 4
  sequencer scenarios + decimals fuzz driven through executeOrder + a live-feed-forced-to-zero → OracleValueZero
  revert. No remediation needed.

- **PR #299 `sprint/v3-p2-signing-keeper` (`689e70f`, 5 commits) — APPROVE-TO-MERGE, 0C/0H, 1 bounded M.**
  EIP-712 v3 typehash/domain byte-identical to the .sol (verified programmatically); version "3", per-chain
  verifyingContract, null everywhere ⇒ v3 signing/creation UNREACHABLE (fail-closed); no v2↔v3 cross-sign
  (on-chain recovery is authoritative). `deriveSigningMinAmountOut` has no 1-wei path (no-feed fallback
  10^max(dstDec−4,0) ≥1 raw unit + decay warning). Server `/api/orders` re-validation caps maxSlippageBps≤500,
  recovers-signer-else-reject, M-07 order_data cross-check, USD dust floor fail-CLOSED (422) on unpriceable-both;
  runs on POST, not API-bypassable. Keeper dual-executor routing never mis-routes (v3-unconfigured ⇒ skip+flag+
  alert, never v2 fallback); order-floor.js + submission-policy.js BYTE-IDENTICAL; KMS only, no ALLOW_PLAINTEXT_KEY;
  signer-guard/kms-signer untouched. Standing: DCA gate untouched, no wagmi-v3, no secrets, SC-04/R1 intact.
  - **M-01 (MEDIUM, bounded, NON-BLOCKING) route.ts:311-320:** the v3 dust-floor scales the DefiLlama leg by an
    unsigned `body.tokenOutDecimals` and combines legs with `max()` (permissive for a floor). For a
    DefiLlama-priceable-but-no-on-chain-feed tokenOut, a compromised client can spoof high decimals to bypass the
    $5 dust floor, leaving the signed min as dust — but realising loss needs client-compromise (the signer) +
    keeper-compromise, the on-chain oracle floor protects all fed pairs, and it is latent (v3 undeployed).
    Remediation: fetch tokenOut decimals on-chain server-side + conservative (min) combine + fail-closed on
    single-source no-feed tokens. Fold into V3-P2 hardening; close BEFORE V3-P4 deploy.
  - **DEVIATION (v3 cancel/invalidate out of scope) — ACCEPT:** client guards fail SAFE (refuse v3 cancel; filter
    v3 out of cancel-all), unreachable today. **HARD PRE-DEPLOY PREREQUISITE for V3-P4: v3 cancelOrder +
    invalidateUnorderedNonces support must land before any v3 executor address is configured** (else users are
    stranded with uncancellable v3 orders). Commits left for owner's SSH batch.


### AUDIT ARBITRUM 46-47 — activation joint gate (2026-07-09) — 🔴 BLOCK

Report: `Audits/Sprint/AUDIT-ARBITRUM-46-47-AUDIT.md`. #303 `sprint/47-arbitrum-activation-prep` `0d9cdb7`
(4 signed commits), base `4277e15`. **VERDICT: BLOCK — 3 HIGH.** #303 must not merge as an activation gate and
the Arbitrum FeeCollector runbook must not be executed.

- **Independent on-chain sweep (2 chainId-42161 RPCs, raw eth_getCode + eth_call, block ~482,185,903)** found
  **9 dead `CHAIN_CONFIGS[42161]` addresses** (zero code): tokens USDT `0xFd086b2F…7D2b` / DAI
  `0xda100097…075e9` / WBTC `0x2F2a2440…3FC30`; sequencer feed `0xFdB631f5…Eff9E`; and ALL 5 Chainlink feeds
  (ETH/USD `0x639Fe6ab…80027`, USDC/USD `0x50834F3e…1147f`, DAI/USD `0xc5C8E77B…40DA7`, USDT/USD
  `0x3f3f5dF8…3E305`, WBTC/USD `0xd0C7101e…41c5`). Controls (WETH, USDC-native, true-USDT/DAI) returned code →
  read path sound. #303 re-verified only ROUTERS; it left the Sprint-46 recon-sourced tokens/feeds/sequencer
  unverified and they are dead — the untrusted-recon failure mode #303 existed to close, still open for
  non-routers.
- **Impact:** dead sequencer feed → fail-closed sequencer gate refuses EVERY Arbitrum quote/swap (503) at
  activation; dead feeds → value/fee-USD oracle non-functional; dead tokens → USDT/DAI/WBTC swaps revert.
  Dead-on-arrival chain. No fund-drain (all fail closed), but rule #3 (on-chain address verification before
  deploy) + "never approve a dead/unverified address" are dispositive.
- **Findings:** H-01 sequencer feed dead; H-02 all 5 Chainlink feeds dead; H-03 3/5 tokens dead; M-01
  regression guard pins routers only (9 dead addrs wrong AND unguarded).
- **#303 got RIGHT (keep):** all router/adapter corrections independently validated LIVE (uniswapV3
  SwapRouter02, sushi RedSnwapper, factory, quoterV2, 1inch, 0x, kyber, openocean, balancer); Augustus V6.2
  whitelist candidate `0x6A000F…001068` live+exact; null-default dark posture; FeeCollector no `.sol` drift;
  input-and-checked whitelist discipline. Recon dead router values confirmed dead (validates #303's fixes).
- **Remediation:** re-recon EVERY non-router 42161 address on-chain (symbol/decimals per token; code+decimals==8+
  description+fresh round per feed; sequencer answers), correct `registry.ts` + `chainlink-feeds.ts[42161]`,
  extend the regression guard to pin all corrected addresses, flag-not-guess the unverifiable — then re-submit
  for a fresh Auditor pass. Fix scoped to 9 addresses + guard; router/plumbing/runbook work is solid.
  Commits + AUDIT-TOTAL block left for owner's SSH batch.

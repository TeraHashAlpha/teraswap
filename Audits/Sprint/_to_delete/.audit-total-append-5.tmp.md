
### AUDIT ARBITRUM 46-47 — RE-RUN @ 2f9f376 (post CHORE-47B) — ✅ APPROVE-TO-MERGE (0C/0H)

Report: `Audits/Sprint/AUDIT-ARBITRUM-46-47-RERUN-AUDIT.md`. #303 `sprint/47-arbitrum-activation-prep` `2f9f376`
(SSH-signed), rebased on `main`. **Prior BLOCK (3 HIGH — 9 dead addresses) FULLY REMEDIATED.** #303 may merge and
the owner may execute `ARBITRUM-FEECOLLECTOR-DEPLOY.md`, subject to one fail-safe MEDIUM runbook amendment.

- **Independent 24-address on-chain re-sweep (2 chainId-42161 RPCs, arb block ~482,322,287, manifest treated as
  a claim):** all 24 have code + correct semantics. Corrected tokens verified — USDT `0xFd086bC7…FCbb9`
  (symbol "USD₮0", 6dec), DAI `0xDA10009c…000da1`, WBTC `0x2f2a2543…C5B0f`. All 5 Chainlink feeds LIVE + FRESH
  (decoded latestRoundData: ETH $1774.81/249s, WBTC $63,918/526s, stables ~$1.00, all 8-dec, description matches
  pair). Sequencer `0xFdB631F5…697D` answer=0 (UP, past grace). Config == manifest == on-chain.
- **Guard now comprehensive:** `arbitrum-manifest.test.ts` diffs all 24 config↔manifest entries (per-entry
  address match + ok-check + category/count), fails CI on any silent edit to any 42161 address. Prior unguarded
  gap closed.
- **USDT0 adjudication = ACCEPT:** (a) dominant/canonical Arbitrum USDT, corroborated on-chain (the USDT/USD feed
  is keyed to this exact address); (b) UI "USDT" vs on-chain "USD₮0" does NOT break the catalog/verified-badge
  guard because the 42161 catalog is empty (dark) — but a `symbolMismatchExempt` entry is required when the
  catalog is populated; (c) no adapter quotes a different USDT (adapters passthrough; mainnet `0xdac17` refs are
  curve-mainnet-only + mainnet health/quorum baselines).
- **Plumbing:** null-default dark ✓; order-engine isolated under activation (ORDER_EXECUTOR_BY_CHAIN has no 42161,
  isDcaLive=8453 only) ✓; clients.ts 42161 present ✓. Runbook: manifest pre-flight HARD GATE (re-verify at a fresh
  block that day) ✓, FeeCollector no `.sol` drift ✓, whitelist input-and-checked ✓, Preview-strictly-before-prod
  ✓, rollback=unset-env ✓.
- **M-01 (MEDIUM, fail-safe):** `CHAIN_TOKENS[42161]` is empty → Arbitrum token selector empty → the runbook's
  Preview WETH→USDC smoke can't run via env-flip alone. Fail-safe (Preview gate blocks a broken prod flip), so
  non-blocking, but activation can't succeed until a catalog-population step (+ the USDT0 allowlist entry) is
  added to the runbook before §6. **L-01:** wstETH absent from the 42161 tokens (confirm intentional).
  Commits + AUDIT-TOTAL block left for owner's SSH batch.

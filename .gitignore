# Dependencies
node_modules/

# Next.js build
.next/
out/
dist/

# Environment (secrets) — NEVER commit any .env file with real keys
.env
.env.*
.env.local
.env.local.*
.env*.local
.env.production
.env.production.local
.env.staging
.env.executor
.env.save
**/.env
**/.env.*
**/.env.save

# Build artifacts
*.tsbuildinfo
next-env.d.ts
contracts/order-engine/build/
contracts/order-engine/cache/
contracts/order-engine/artifacts/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Vercel
.vercel/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Scripts & tools (local dev only — never deployed)
scripts/*.sh
tools/
TeraSwap_OrderEngine_SecurityAudit.pdf

# Internal docs (infrastructure, deployment guides)
TeraSwap_Infrastructure_Guide.docx
TeraSwapOrderExecutor_DEPLOY.sol

# Audit remediation docs (keep PDFs, remove internal docs)
Audits/*.docx

# Order Engine deployment secrets
.deployer-*.json
.env.gelato
.env.executor
contracts/order-engine/deployment-*.json
contracts/order-engine/executor/.env.executor
contracts/order-engine/executor/node_modules/
.vercel
contracts/order-engine/.env

# Capacitor native builds (generated, large, device-specific)
ios/App/Pods/
ios/App/build/
ios/App/App/public/
android/app/build/
android/.gradle/
android/app/src/main/assets/public/

# Security: prevent accidental commit of key material
*.pem
*.key
credentials.json

# Endpoint baseline overrides (local testing only)
data/endpoint-baseline-overrides.json

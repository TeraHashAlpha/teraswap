# Dependencies
node_modules/

# Next.js build
.next/
out/
dist/

# Environment (secrets) — NEVER commit any .env file with real keys
.env
.env.*
.env.local
.env.local.*
.env*.local
.env.production
.env.production.local
.env.staging
.env.executor
.env.save
**/.env
**/.env.*
**/.env.save

# Build artifacts
*.tsbuildinfo
next-env.d.ts
contracts/order-engine/build/
contracts/order-engine/cache/
contracts/order-engine/artifacts/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Vercel
.vercel/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Scripts & tools (local dev only — never deployed)
scripts/*.sh
tools/
TeraSwap_OrderEngine_SecurityAudit.pdf

# Internal docs (infrastructure, deployment guides)
TeraSwap_Infrastructure_Guide.docx
TeraSwapOrderExecutor_DEPLOY.sol

# Audit remediation docs (keep PDFs, remove internal docs)
Audits/*.docx

# Order Engine deployment secrets
.deployer-*.json
.env.gelato
.env.executor
contracts/order-engine/deployment-*.json
contracts/order-engine/executor/.env.executor
contracts/order-engine/executor/node_modules/
.vercel
contracts/order-engine/.env

# Capacitor native builds (generated, large, device-specific)
ios/App/Pods/
ios/App/build/
ios/App/App/public/
android/app/build/
android/.gradle/
android/app/src/main/assets/public/

# Security: prevent accidental commit of key material
*.pem
*.key
credentials.json

# Endpoint baseline overrides (local testing only)
data/endpoint-baseline-overrides.json

# Cloudflare Workers node_modules
workers/*/node_modules/

# ── [CHORE-DOCS-HOUSEKEEPING 2026-06-05] tooling / caches / vendored / runtime ──
# Generated caches & reports (the CURATED audit trail lives under Audits/** and is committed)
/cache/
/contracts/cache/
/health-reports/
/reports/
# Agent / Claude / hallmark tooling + runtime (project skills under .claude/skills/*.md stay tracked)
/.agents/
/.hallmark/
/.claude/scheduled_tasks.lock
/.claude/worktrees/
/.claude/wf-full-audit.js
/.claude/skills/gsap-*
/.claude/skills/defi-incident-comms
/.claude/skills/hallmark
/skills-lock.json
/sprint-16-goal.html
# Vendored ERC-7730 clear-signing registry (upstream, not authored here)
/clear-signing-erc7730-registry/
# Stray root foundry workspace — the real contracts build is under contracts/ (its own foundry.toml)
/foundry.toml
/remappings.txt
/lib/
# Generated worker lockfile (package.json is tracked; lock left out per existing convention)
/workers/monitor-tick-cron/package-lock.json

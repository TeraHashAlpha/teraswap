# FIXTURE (CHORE-POLISH-4 P1): FAKE bare-hex EVM private key — NOT a real credential.
# Proves the `evm-private-key-keyword-proximity` rule fires on a `KEYWORD = 0x<64hex>` shape.
# This exact fake value is allowlisted by value in .gitleaks.toml so the committed fixture
# does not turn the CI git-history scan red; ANY OTHER 64-hex key here would still flag.
PRIVATE_KEY = 0xdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef

#!/usr/bin/env tsx
/**
 * SUPERSEDED by scripts/token-catalog/build.ts (CHORE-TOKEN-CATALOG-PIPELINE).
 *
 * This was the never-implemented CoinGecko category-sync stub. `npm run tokens:sync` now
 * runs the real multi-source cross-verified catalog pipeline, which also merges
 * scripts/token-category-overrides.ts into the generated catalog's categories.
 * Kept per repo rule #4 (never delete files — git history preserves everything).
 */

console.log('SUPERSEDED: `npm run tokens:sync` now runs scripts/token-catalog/build.ts')
console.log('(the multi-source cross-verified catalog pipeline — see docs/Prompts/CHORE-TOKEN-CATALOG-PIPELINE.md)')
process.exit(0)
